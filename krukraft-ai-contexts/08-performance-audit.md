# Krukraft — Performance Audit

## Summary

The public paths now have deliberate performance engineering in place:

- Upstash Redis + `unstable_cache` + single-flight dedup
- Leaner select projections for listing/detail shells
- RSC streaming decomposition on the resource detail page
- Root layout no longer reads the authenticated server session on every request
- Root theme bootstrap now paints the resolved `system` theme by default and applies stored light/dark overrides before hydration, which preserves system-following first paint without waiting for the client theme effect
- `UserPreference.theme` now defaults to `system` at the Prisma/database layer too, so runtime bootstrap and newly created stored preferences no longer disagree about the baseline theme
- the settings preference form now locks the currently rendered runtime theme into `localStorage` the first time a user opens `/dashboard/settings` without an existing stored theme, so the page no longer appears to auto-flip to an older DB-backed theme preference on entry
- local browser debugging now has a repo-owned Playwright API probe path (`npm run browser:probe`) for launch, discover/library transitions, and settings-theme verification; this exists because the current macOS environment can still abort during `playwright test` browser launch even when direct Playwright API launch succeeds
- the same local/browser verification policy now has explicit operator aliases in
  `package.json`: `test:e2e:local`, `test:e2e:local:headed`, and
  `test:e2e:local:ui` keep Chromium pinned to `BASE_URL=http://127.0.0.1:3000`
  so reruns do not depend on ad-hoc shell env wiring
- dashboard/browser verification is now split into two repo-owned probe surfaces: `npm run browser:probe:dashboard` still covers the main cross-route dashboard transitions (downloads, purchases, settings) after entering the dashboard shell, while `npm run browser:probe:management` now adds hard-refresh shell checks for the main dashboard, creator, and admin entry routes so wrong-level app-root fallback regressions are not limited to manual repeated-refresh testing
- the GitHub Actions browser-smoke workflow now treats cloud CI as Chromium-only on purpose: it caches the Playwright browser bundle at `${{ github.workspace }}/.cache/ms-playwright`, installs only Chromium, and disables the local-only WebKit fallback path so `Install Playwright Browsers` does not keep re-downloading or provisioning an unused browser on every run
- green Browser Smoke runs are now expected to stay quieter too: CI suppresses repeated R2-local-storage fallback warnings, repeated `getUserPreferences()` default-row warnings, and the non-critical `/resources` discover/detail timeout fallback warnings that otherwise flood logs without changing route correctness
- the repo-owned browser probe now also samples public ↔ dashboard navigation coverage directly: the `resources-to-library` and `library-to-resources` scenarios record `data-loading-scope` frames and fail on blank-gap transitions, so dashboard-shell regressions are no longer limited to the heavier Playwright spec path
- the root layout now also mounts a lightweight `ResourcesEntryNavigationOverlay` for cross-group jumps into `/resources`, so dashboard/library → marketplace transitions can hold a `resources-browse` loading scope before the resources route group mounts and the repo-owned browser probe can catch missing coverage instead of silently skipping that gap
- `/resources` now avoids duplicate cold-path discover work too: the streamed `lead` and `collections` sections still resolve separately at the UI layer, but they now share one cache-backed homepage bundle so a cold request does not fetch two separate resource pools for the same discover shell
- `/resources/[slug]` now also shares one cache-backed detail bundle for shell/body/footer/purchase-meta reads, cutting repeated same-row Prisma fetches from the cold detail path before trust/review/related branches even start
- both public routes now trust their structural `Suspense` fallbacks more than hard sub-second timeout races: discover lead/collections and detail secondary branches no longer degrade early to fail-soft timeout fallbacks during a normal slow-but-healthy stream, which keeps targeted resource smoke runs cleaner and lets the real streamed sections complete
- the same probe layer now has a `dark-theme-logo` scenario that forces dark first paint, delays dark-logo asset delivery, and asserts the dark fallback layer stays visible instead of briefly exposing the light-theme logo while the custom asset is still loading
- the same repo-owned browser-probe layer now also covers direct-load public/admin informational pages (`npm run browser:probe:pages`) plus authenticated admin-analytics and creator-management flows (`npm run browser:probe:management`); GitHub Actions now runs the management probe in its own `Management Browser Probes` job, separate from the main `Browser Smoke` job, so management-route verification still executes even if the primary smoke bundle fails before later probe steps
- the same management/browser-probe layer now also has overview, library, downloads, purchases, settings, and membership hard-refresh shell checks plus creator hard-refresh shell checks, so the canonical `/dashboard/*` family can catch brief app-root fallback regressions as runtime verification classes instead of relying on manual repeated refreshes
- `/dashboard/settings` now avoids a write-backed preference read on entry: the page streams the settings sections behind an in-page `Suspense` boundary, while `getUserPreferences()` became a read-only `findUnique()` + defaults path instead of issuing `user.findUnique()` and `userPreference.upsert()` on every page load
- the canonical `/dashboard` overview, settings, membership, library, downloads, and purchases routes now bypass the old DB-backed creator-access shell blocker by resolving shell identity from the signed JWT token snapshot first instead of waiting on the retired dashboard-lite creator checks before first shell render
- `DashboardAppShell` now also keeps its shared viewer payload on the token-snapshot path: the shell no longer waits on creator profile reads just to build shared nav chrome, `INSTRUCTOR` users resolve to creator mode without an extra DB round-trip, and only the remaining non-admin, non-instructor branch still falls back to `getCreatorAccessState()` when the shell genuinely needs to distinguish creator apply-vs-full nav state
- `/dashboard/library`, `/dashboard/downloads`, and `/dashboard/purchases` no longer block their full page shell on purchased-item queries; those canonical routes render the route shell after `requireSession()`, the library route splits a light surface-summary read (`owned / last added / continue`) from the heavier recovery + toolbar + result-grid payload, the downloads route uses a cheap download-history summary (`count / latest download`) plus a capped recent-row window instead of reading the user's entire download history on every dashboard visit, and the purchases route now does the same with a purchase-history summary (`order count / completed count / total spent`) plus a capped recent-row ledger window instead of materializing the full user order history on every dashboard visit
- `/admin/reviews` and `/admin/creators` now also render their route shell first and stream the heavy moderation/application tables through in-page `Suspense` fallbacks instead of waiting on those admin lists before first render
- `/admin/tags` now also renders its shell before the tag-management client/table surface resolves, and `/admin/activity` now relies on the admin layout gate instead of repeating a page-level admin-session read
- `/admin/settings` now also renders the admin shell immediately and streams the heavy platform-settings form behind an in-page `Suspense` fallback, so the route no longer shows a full-page loading block while live platform config resolves
- creator protected analytics, sales, and profile routes now also render their workspace shell before the heavier KPI/table/form reads finish, moving those branches behind creator-specific `Suspense` fallbacks so first shell paint no longer waits on the slowest creator query in the route
- the same management/browser-probe layer now also has admin-overview and admin-analytics hard-refresh shell checks, so the main `/admin` family entry surfaces can prove they stay inside the admin loading family instead of briefly surfacing the app-root fallback on repeated refreshes
- the same management/browser-probe layer now also has a creator resource-editor hard-refresh check, so `/dashboard/creator/resources/new` proves the dedicated `dashboard-creator-resource-editor` route-ready marker instead of relying only on the generic creator-shell probe
- historical note from the pre-hard-cut creator dashboard family: the same management probe surfaced a real creator-analytics runtime bug on 2026-04-06 when `/dashboard/creator/analytics` was still in CI coverage; one creator repository raw query still joined `"ResourceStat"` instead of Prisma's mapped `"resource_stats"` table
- the GitHub Actions browser-smoke workflow now explicitly enables the Postgres `pg_trgm` extension before `prisma db push`; CI does not replay raw migration SQL during `db push`, so the extension must be provisioned directly or the search/recommendation smoke routes fail on `similarity(...)`
- verification policy is now stricter at close-out time: Browser Smoke and post-deploy warm/perf runs are not considered "clean" from workflow status alone; operators are expected to review logs for hidden `flaky`, `retry #`, timeout, and threshold-failure signals before calling a run stable
- Sentry is now part of the repo-owned first-pass observability baseline too:
  Next.js build/runtime wiring lives in `instrumentation*.ts`,
  `sentry.*.config.ts`, and `withSentryConfig(...)`, but current scope remains
  error capture plus basic tracing rather than replay/profiling-heavy
  observability
- CPD close-out now also has an explicit deployment-evidence guardrail: do not call `commit + push + deploy` complete from `git push` output alone; the canonical check is `npm run cpd:verify`, which requires the canonical GitHub remote, `HEAD == origin/main`, and a GitHub deployment record for the pushed commit
- Post-deploy warm-cache + smoke perf workflow, with a manual `workflow_dispatch` fallback for CLI-driven deploys
- Warm workflow installs now retry `npm ci` and preserve install logs as artifacts, which makes failed warm runs debuggable even when the job dies before `warm-cache.log` exists
- The same post-deploy workflow now installs on Node 24 instead of Node 20 so GitHub Actions uses the same lockfile/npm resolution behavior that currently passes locally
- GitHub-maintained actions inside that workflow are now on `checkout@v6`, `setup-node@v6`, and `upload-artifact@v6`; the remaining Node 20 deprecation warning surface is expected to come from `grafana/setup-k6-action@v1` until Grafana publishes an explicitly updated action/runtime line
- Optimizer-compatible preview images now stay on Next Image by default, with selective bypass only for sources that are not safely optimizable
- Above-the-fold marketplace hero, spotlight, and card images now use targeted eager loading instead of blanket eager behavior
- Build-safe platform config on branding-only build paths
- favicon resolution is now pinned to the dedicated repo favicon asset when platform settings are missing or degraded, so shared platform config no longer substitutes the full logo or icon logo as the runtime favicon

Primary bottleneck class is now:

**client personalization latency after hydration plus global client-JS overhead on public routes and cold-instance cache misses on heavy query paths**, not basic bundle size or simple rendering overhead.

Current perf-hardening baseline for the production UX initiative is:

- prioritize `/resources`, `/resources/[slug]`, `/creators/[slug]`, `/categories/[slug]`, and the warmed listing-control routes before broader infra changes
- treat warm failures as route-class incidents, not generic "deploy was slow" noise
- keep current p95 budgets as the baseline while cache-path and warm-shape mismatches are still being removed
- prefer code/cache/streaming fixes first; infra changes should only be discussed after the same route still shows tail spikes once warm targets and cache layers are aligned

---

## Important Fixes Already Landed

- `npm run build` no longer runs `prisma migrate deploy`
- post-deploy warm + smoke workflow is active, and can now be triggered manually when production deploys bypass GitHub deployment events
- resource detail route was decomposed into shell + deferred purchase/body/footer/review/related paths
- `/resources/[slug]` now keeps the route-entry shell cache separate from the metadata cache, so the blocking detail-shell read no longer carries the long-form description field that already streams later in the body section
- optional session work was pushed off several anonymous critical paths
- public remote preview image delivery now keeps optimizer-compatible hosts on Next Image, while still bypassing the optimizer for GIFs, non-HTTPS assets, and non-allowlisted hosts
- build-time Prisma warning from `platformSettings.findFirst()` was removed by separating build-safe platform config from admin live config
- root layout stopped SSR-seeding `SessionProvider` from `getServerSession`, which restores a cleaner caching baseline for public routes
- the root client provider tree no longer mounts `SessionProvider`; auth-aware navbar/pricing/checkout UI now use a smaller `/api/auth/viewer` fetch instead of the global NextAuth client-session baseline
- route-group navigation overlays are now narrower than the older legacy stack: the root layout keeps `ResourcesEntryNavigationOverlay` for public/dashboard → `/resources` jumps and a shell-only `DashboardEntryNavigationOverlay` for public → `/dashboard/*` jumps. The dashboard bridge must not own generic content skeletons; those remain route-owned inside the mounted dashboard shell
- the full `HeroSearch` client bundle is now lazy-loaded on secondary public routes (support, category, creator, membership, resource-detail, and error/not-found surfaces), while loading shells now render the structural search placeholder directly instead of importing `HeroSearch`
- the old hero-search preview/bones surface was later retired entirely after discover mode proved `HeroSurface` owns the live hero shell; runtime marketplace search now keeps only the listing/navbar contract in `HeroSearch`
- the dashboard settings route now again renders its intro shell before profile/preferences data resolve and streams the settings sections behind an in-page `Suspense` boundary, while the route-level loading shell and targeted settings proofs stay aligned with the current interactive settings surface (overview panel + live profile/preferences/notifications + account-access rail) instead of the earlier read-only summary layout
- `/dashboard/settings` and `/dashboard/membership` are protected as canonical dashboard account routes, which removes the old page-level auth gate from the first shell render path and prevents entry handoff from waiting on a legacy redirect check before any route-ready marker can appear
- auth and billing return paths are now aligned to that same hot account
  family: `/auth/login` falls back to `/dashboard`, the default Google
  completion path for login/register lands on `/dashboard/library`, and Stripe
  membership success returns to `/dashboard/membership`, so post-auth and
  post-checkout entry no longer bounce through retired dashboard surfaces first
- the dashboard settings data branch now fail-softs for transient profile/preferences infrastructure issues by falling back to session-backed profile basics and default user preferences after a bounded wait, so the in-page settings skeleton no longer waits indefinitely on the DB path during temporary pool pressure
- `/dashboard/settings` and `/dashboard/membership` now also resolve their hot user snapshot from the signed JWT token before falling back to `getServerSession()` inside the page branch, removing the second NextAuth session callback hop from the common account-page path
- public account-menu transitions into `/dashboard/settings` and `/dashboard/membership` no longer request a legacy full-screen dashboard entry overlay; the dashboard shell now clears handoff from canonical shell/route markers directly
- the canonical `/dashboard`, `/dashboard/settings`, `/dashboard/membership`, `/dashboard/library`, `/dashboard/downloads`, and `/dashboard/purchases` routes keep the same dashboard loading family markers while skipping the creator-access DB wait at layout entry
- the `/dashboard/membership` results fallback now uses the same hero/stat/billing geometry as the final membership surface instead of a generic stacked placeholder, reducing visible skeleton shift on the membership route while its streamed branch resolves
- design-system page layout primitives now forward arbitrary DOM props to their root nodes, which restores route-ready markers on pages that attach `data-route-shell-ready` directly to `PageContent` / `PageContentNarrow` and prevents hidden regressions where a route renders correctly but never exposes its shell-ready contract to overlays or browser probes
- the entire `src/app/admin` subtree is now explicitly `force-dynamic` at the layout boundary, so production builds no longer attempt to prerender `/admin` and other admin pages with live Prisma reads during `next build`, which previously caused deployment failures and skipped post-deploy warm/perf runs before they even started
- `/admin` overview now lets the admin layout own the auth/session gate while the page fetches platform metrics and recent activity in parallel; the metrics shell renders first and the recent-activity table streams behind a structural fallback, so hard-refreshes do not wait on the full overview payload before any in-page admin content appears
- `/admin/analytics` now avoids blocking the full page on one `getPlatformMetrics()` bundle: summary counts and 30-day KPIs resolve from a dedicated cache surface first, while reporting charts and top-resources stream later behind a structural analytics-reporting skeleton
- `/admin/resources` now resolves category/filter data separately from listing rows and purchase summaries, so the admin resources shell can render header + filter controls before the heavier resource table section finishes; the table/empty/pagination branch now has its own structural fallback instead of holding the full page
- `/admin/orders` now lets the admin layout own the auth/session gate while the route keeps its filter toolbar in the first shell; stats cards and the orders table stream later behind a structural orders-results fallback so hard refreshes do not wait on the combined order list + aggregate payload before any in-page admin UI appears
- `/admin/users` now lets the admin layout own the auth/session gate while the route keeps its header + search toolbar in the first shell; the user table streams later behind a structural users-results fallback so hard refreshes do not wait on the user lookup query before any in-page admin UI appears
- the creator protected layout now acts as the single creator-access gate for admitted workspace routes; creator overview, creator resources, and the new-resource entry page no longer pay an extra `getCreatorAccessState()` read after that layout has already resolved the same access decision
- creator protected overview and resource-management routes are now also part of the shell-first hardening sweep: `/dashboard/creator`, `/dashboard/creator/resources`, `/dashboard/creator/resources/new`, and `/dashboard/creator/resources/[id]` render shell/actions first and stream heavier KPI, table, and editor branches separately, so creator workspace perception is less coupled to the slowest query in each route
- creator protected pages that still need a user id now resolve it from the signed JWT snapshot before falling back to `getServerSession()`, so creator overview/analytics/resources/profile/sales/editor pages do not repeat the full NextAuth session callback path after the protected creator layout has already admitted the same user
- creator route overlays no longer wrap creator loading shells inside `DashboardGroupLoadingShell`; creator-family handoffs now keep only the creator shell itself, which removes the brief “dashboard in dashboard” flash when switching into creator profile/analytics/resources/editor routes
- the signed-in `/resources` personalized discover section now sits behind a dedicated lazy client boundary with a matching structural skeleton, so the main discover route no longer ships recommendation-section client logic in its initial payload
- public-route render hardening on 2026-04-08 narrowed the `/resources` viewer-state provider to viewer-aware card/personalization subtrees, so browse headings/filter chrome and other static server content no longer sit inside the owned-state hydration boundary
- the same pass also changed marketplace listing reads to overlap cached category-sidebar loading with the primary listing query whenever category-id resolution is not needed up front, removing a serial await from the common unfiltered listing/search paths
- the follow-up pass on 2026-04-08 moved `/resources` no-results search recovery behind a nested server `Suspense` boundary and out of the viewer-state grid path, so search-empty shells can stream before taxonomy recovery suggestions finish and that branch no longer hydrates the owned-state grid just to show recovery links
- the same pass narrowed `/resources/[slug]` detail viewer-state to the success/purchase/owner-review subtree and switched detail metadata to the lighter metadata reader, so the public detail shell avoids both an unnecessary full-payload metadata read and a route-wide personalization wrapper
- a follow-up client-payload pass on 2026-04-08 now lazy-loads the `/resources/[slug]` owner-review form only after base viewer-state confirms the viewer owns the resource, keeping that review-form bundle off anonymous and non-owner detail visits
- a later hydration-scope pass on 2026-04-08 moved `/resources` listing owned-state hydration into `ResourceGrid`'s deferred owned-id hydrator, so the main marketplace grid no longer sits under `ResourcesViewerStateProvider`; the route now keeps that provider only around the single spotlight card while the grid shell streams and patches owned badges afterward
- another discover-path follow-up on 2026-04-08 restored a server-rendered default "Top picks" section on `/resources` and only lazy-loads the heavier personalized discover module for authenticated viewers, so anonymous traffic no longer waits on that client bundle just to see the first recommendation row
- the next follow-up on 2026-04-08 moved `/resources` discover `Trending now` out of the shared personalized provider subtree and lets that section patch owned badges through its own deferred base-state fetch, trimming the remaining viewer-state boundary in discover mode down to the personalized module itself
- the next `/resources` root-cause pass on 2026-04-08 split the discover server payload itself: the route now starts cached `lead` and `collections` readers in parallel, renders the quick-browse shell immediately, and streams the curated card rows through separate structural `Suspense` boundaries instead of awaiting one monolithic `getDiscoverData()` bundle before any discover section can unlock
- the compatibility `getDiscoverData()` reader now composes those split discover caches for viewer-state and warm callers, and public-cache warm now seeds the same split discover readers that the route consumes instead of warming only the older combined payload
- LHCI now samples the key `/resources` routes twice per run and treats the overall performance category, total blocking time, and cumulative layout shift as blocking floor assertions instead of advisory warnings, so obvious lab regressions fail faster before they hide behind a single noisy pass
- the post-deploy warm/perf workflow summary now emits a rollup with pass/fail counts, the worst p95 route, and the route closest to its budget, so production perf regressions are easier to triage from the GitHub Actions summary without downloading artifacts first
- the GitHub Actions perf-summary append step now uses a parser-safe inline `node --input-type=module -e ...` command instead of a heredoc block, after a broken YAML parse on push proved the previous summary script was too fragile inside `run: |`
- the repo now also has a repo-owned workflow syntax gate: `scripts/check-workflow-syntax.mjs` parses every file under `.github/workflows/`, and `npm run lint` calls it so GitHub Actions YAML regressions are caught before push/CI parser failures
- the post-deploy warm step now re-hits `/resources` twice on purpose, because production checks on 2026-04-07 showed that the first warm hit for the public home shell can still take ~10s immediately after deploy even when the steady-state smoke budget passes afterward
- repo-owned browser probes now test transition/loading fidelity too, not just URL/heading success; public ↔ dashboard transitions are expected to show a matching loading scope with no blank `main` gap, and dark-theme brand chrome is expected to keep a dark fallback visible until any delayed custom dark asset finishes loading
- `/api/auth/viewer` now reads the signed JWT cookie through `next-auth/jwt` instead of `getServerSession`, which removes a Prisma-backed session lookup from the lightweight public auth-chrome path
- the marketplace/detail private viewer-state APIs now read the same signed JWT snapshot instead of `getServerSession`, which removes another public-route session lookup from the Prisma pool before owned-state/review queries begin
- the auth-viewer hook no longer hydrates from module cache during the initial render, which avoids navbar server/client drift; navbar auth controls now hold their footprint with lightweight loading placeholders until the viewer request settles
- public navbar auth resolution now defers to browser idle time and warms on interaction, which trims eager auth fetch pressure on public routes while keeping auth-aware chrome responsive when users engage it
- local dev/HMR transient auth-viewer network failures now fall back silently to the signed-out snapshot instead of spamming `[AUTH_VIEWER_HOOK] Failed to fetch`, which keeps browser/dev logs usable while preserving real production/non-transient error reporting
- `/resources/[slug]` detail viewer-state still defers auth-viewer resolution to idle time so anonymous detail visits avoid eager auth churn, but signed-in `/resources` discover hydration now restores auth-viewer state from persisted browser session storage and can settle immediately on hard refresh when the same tab already knows the viewer
- `/resources` stopped reading cookies/session at the page level; auth-aware owned badges and signed-in discover sections now hydrate from `/api/resources/viewer-state`
- `/resources` marketplace viewer-state is now split into `scope=base` and `scope=discover` so owned badges can hydrate before heavier recommendation payloads
- `/resources` base viewer-state now uses a short-lived browser cache keyed by viewer id, reducing repeat owned-badge fetches on quick signed-in marketplace navigations without risking cross-user bleed
- the signed-in `/resources` discover island now also hydrates its auth-viewer snapshot and short-lived `scope=base` / `scope=discover` viewer-state payloads from session storage when available, so hard refreshes in the same tab can reuse the previous personalized payload instead of repainting the generic fallback first
- the same discover-island cache path now avoids a React function-initializer trap too: the cached personalized section component is stored behind `useState(() => cachedComponent)` instead of passing the component function directly, which prevents React from invoking the section with `undefined` props on return navigation and removes the brief `/resources` route-error flash that had started appearing after detail → discover hops
- the same `/resources` discover client path no longer waits for the owned-id `scope=base` fetch to finish before starting `scope=discover`; recommendation hydration now runs as soon as auth readiness is known, removing the old `auth -> base -> discover` client waterfall on refresh
- `/resources` signed-in discover payloads now use short-lived private Redis + `unstable_cache` reuse so repeat navigations do not recompute the full recommendation state every time
- purchase-derived learning profiles used by signed-in discover hydration now also go through Redis + single-flight, reducing cross-instance rebuilds of the same viewer profile
- personalized discover now also pushes user-interest profiles and shared Phase 2 candidate pools through Redis + single-flight, which cuts cross-instance recomputation when recommendation requests fan out
- when a cold discover refresh hits Prisma pool pressure, section loaders now stop and let the outer best-effort discover response degrade for that request instead of spending extra fallback DB queries; discover collection-source loading is now sequential in every environment, not just local development, so `/resources` home does not multiply cold pool pressure across multiple section-source queries on small production pools
- the `/resources` catalog-controls bar now also treats category chips as a best-effort shell enhancement: the route still prefers the cached discover category list, but after a short timeout or transient DB/pool failure it falls back to the fixed marketplace taxonomy instead of letting the navbar controls wait on the same Prisma pool as discover home sections
- the `/resources` route entry now also catches any remaining transient Prisma/pool failures that escape lower-level listing/discover guards and renders a route-level temporary-unavailable state inside the marketplace shell, so short Supabase pooler incidents do not have to collapse straight into `src/app/resources/error.tsx`
- the category-chip timeout budget is now slightly wider (`1000ms` instead of `400ms`), and the timeout/best-effort warnings stay dev-silent by default so local first-hit `/resources` compiles do not flood the terminal while production warning visibility remains intact
- recommendation impressions are no longer written from the cached discover miss path; they now fire from a client-side section exposure tracker through `/api/recommendations/impression`, which keeps the cached discover loader read-only and aligns analytics with actual viewport exposure
- marketplace discover personalization now also treats recommendation-path transient DB failures as best-effort and can return `null`/empty secondary personalized sections instead of failing the private viewer-state route under pool pressure
- the private `/api/resources/viewer-state` and `/api/resources/[id]/viewer-state` routes now mirror that same transient fail-soft policy by returning safe empty/null payloads instead of 500s when pooler reachability blips, which removes a chunk of signed-in return-navigation error noise even when a background viewer-state read cannot complete
- the same return-navigation hardening now has a client recovery layer too: `Logo` forces a document navigation for `/resources/[slug]` → `/resources` returns, and the `/resources` route error boundary now converts its one-shot auto-retry into a full reload of the current `/resources` URL instead of a soft `reset()` rerender. This is intentionally narrow to the resources family because the issue class has been transient route-cache / pool-pressure fallout during immediate back-to-discover flows rather than a repo-wide navigation policy problem
- the same resources-family recovery path now also covers browser-back returns and hides the manual error card during successful autoretry: detail → discover returns can force a resources overlay even without an explicit navigation signal, that overlay and the route-ready clearer both wait briefly for a discover-personalization ready marker, and the route error boundary now shows the standard discover skeleton while the one-shot hard reload is in flight
- public trust-summary revalidation is now best-effort under transient pooler pressure too; if `review.aggregate` or `purchase.count` loses DB reachability while `getResourceTrustSummary(resourceId)` is revalidating, the helper falls back to `null/0/0` trust values and logs a targeted fallback warning instead of escalating the browse route or spamming repeated hard errors
- repeat personalized client JSON fetches now use a small browser-side TTL/dedupe cache, reducing quick remount/refetch churn for discover and owner-review sections
- the discover hero is now a fixed repo-owned banner contract, which removes the older hero resolver/cache/analytics path from the public marketplace route entirely
- `/brand-assets/*` runtime delivery now guards against self-referential alias values and falls back to concrete default assets instead of redirecting a logo/icon request back to the same runtime route
- `/brand-assets/*` runtime alias routes now also bypass `src/proxy.ts`, removing unnecessary proxy/ranking-cookie overhead from logo/favicon requests
- the `/resources` filter sidebar no longer wraps simple `router.prefetch(...)` work in `startTransition()`, which removes the small hover-time pending flicker that used to dim/pointer-block the sidebar during filter warmup
- trigram index coverage now extends beyond `Resource.title/description` to `Category.name`, `Tag.name`, `User.name`, and `User.email` so marketplace search and admin user lookup avoid the remaining text-search scan hotspots
- marketplace search and live search now share a broader weighted relevance query across title, slug, description, category, tag, and creator fields instead of using narrower title/description-only listing filters
- search now tokenizes multi-word queries and expands a small synonym/alias set for common study terms, which improves recall without adding a separate search engine
- synonym groups, recovery fallback terms, relevance boosts, and match-reason copy now live in a shared config module, which makes search tuning cheaper and less error-prone than editing multiple code paths
- the search config is now driven by typed term rules plus shared weight/copy maps, which lowers the risk of helper/repository drift when tuning synonyms, fallbacks, or ranking behavior
- search result pages default to `Best match` when a query is present, while still allowing alternate marketplace sort orders for the matched set
- shared public search bars now debounce suggestion fetches and always route full-result queries back to canonical `/resources` results, avoiding the old pattern of appending `?search=` to unrelated public routes
- typeahead suggestions now use an explicit lightweight `/api/search?view=suggestions` mode, which trims the ranked-search result shape for dropdowns and avoids extra review-count work that live suggestions never display
- typeahead search now reuses a short-lived browser-side suggestion cache, and no-result dropdown recovery now calls a dedicated `/api/search/recovery` endpoint so the ranked search query is not executed twice for the same miss
- ranked search results and recovery payloads now sit behind short-lived `unstable_cache` plus Redis + single-flight layers on the backend, cutting duplicate work both on warm instances and across repeated public queries
- the shared ranked-search SQL no longer couples every result page to `COUNT(*) OVER()`; live search now reads ranked rows only, and marketplace search computes totals in a separate path when needed, which reduces unnecessary window-count work on the hottest search requests
- the ranked-search SQL now stages a cheaper candidate-resource filter before it computes `tag_metrics` via lateral aggregation, which removes one of the biggest remaining per-query costs from obvious title/category/creator matches
- trigram index coverage now also includes `Resource.slug`, `Category.slug`, and `Tag.slug`, so the ranked query's slug-based `ILIKE` and similarity branches are no longer leaning only on btree uniqueness indexes
- marketplace cards, search dropdown thumbnails, and resource preview galleries now use a simplified shared image primitive that keeps images visible by default and relies on the container background for placeholder treatment, eliminating the class of "image fetched but still hidden until refresh" regressions caused by JS reveal state drift
- optimizer-compatible remote preview images no longer bypass Next Image automatically, the app now allows AVIF alongside WebP, and above-the-fold marketplace card surfaces can opt specific images into eager loading to reduce residual LCP pressure without over-eagering the whole catalog
- local browser reruns on 2026-04-02 stopped reproducing the old Next dev `loading="eager"` advice on `/resources`, `/resources?search=worksheet`, and `/resources/[slug]` after widening the eager window for query-driven listings, carrying eager preview URLs forward across repeated discover/personalized cards, and marking the active duplicate-src detail thumbnail eager/high alongside the main priority image
- the same public search endpoints now declare short-lived shared cache headers in source, but production verification on 2026-04-01 only surfaced `Cache-Control: public`, so CDN/edge response caching is still an open follow-up rather than a verified gain
- no-result search UX now recovers with alternate query suggestions plus category/tag browse links, reducing dead-end searches without adding a separate search backend
- `/resources` search headings now render search-specific copy instead of falling back to browse headings on uncategorized searches
- local verification now has a repo-owned `npm run smoke:local:search` path that checks the search results page, no-results recovery page, `/api/search`, search recovery, and `/api/auth/viewer` sequentially with retries, avoiding the flakiness of ad-hoc localhost curls in constrained environments
- the shared smoke path now gates on `/api/internal/ready` before deeper route/API assertions, which separates server-readiness problems from search/auth regressions and makes local verification more stable
- `npm run smoke:prod:search` now reuses the same verification flow against `https://krukraft.com`, while preview deployments can use `BASE_URL=<preview-url> npm run smoke:search`
- Playwright now provides a browser-level smoke layer (`npm run test:e2e`) for catching hydration/image regressions that route/API smokes and k6 cannot see; the local project still uses the `chromium` project name, but it now defaults to `channel: "chromium"` so local verification uses Playwright's bundled Chromium browser instead of the headless shell or installed Chrome stable. Use `PLAYWRIGHT_BROWSER_CHANNEL=chrome` only when you intentionally need installed-Chrome verification
- browser-level coverage now includes `/resources`, top-bar search submit into canonical `/resources?search=...`, canonical search results, no-results recovery, resource detail image rendering, and authenticated preview-image uploader flows on `/admin/resources/new` plus `/dashboard/creator/resources/new`
- `npm run smoke:local:browser` is now the repo-owned pre-merge Playwright smoke bundle for those key public/auth/uploader flows, and Playwright defaults its local base URL to `http://127.0.0.1:3000` so the smoke path can bring up the dev server itself
- `.github/workflows/browser-smoke.yml` now adds a cloud browser-smoke lane on GitHub Actions: Ubuntu runner, Postgres service, `prisma db push`, `db:seed`, Playwright browser install, then lint + typecheck + `npm run smoke:browser:ci`
- Root-layout Vercel Analytics / Speed Insights script injection is now production-only, so local dev and GitHub Actions browser smoke runs no longer emit cross-origin telemetry load noise that can mask real UI/runtime failures.
- The global security-header profile is now split by environment: production keeps HSTS and CSP `upgrade-insecure-requests`, while local dev / GitHub Actions browser smoke skip those directives so localhost asset redirects are not silently rewritten to `https://localhost` and misreported as UI/runtime regressions.
- `npm run smoke:browser:ci` intentionally focuses the cloud gate on public/auth/navigation/settings browser coverage and skips uploader specs until the CI environment owns storage credentials/config that match those upload routes
- the repo now also has first-party hooks for accessibility and perf auditing at the browser layer: `@axe-core/playwright` can be used inside Playwright specs, `@lhci/cli` is configured through `.lighthouserc.json` for key `/resources` routes, and `@next/bundle-analyzer` is wired behind `npm run analyze`
- 2026-04-02 bundle follow-up removed `framer-motion` from the admin notification stack entirely, replaced `next-auth/middleware` in `src/proxy.ts` with direct `next-auth/jwt` checks, and switched preview-image drag/drop upload to a native file-input + drag/drop implementation behind a lazy client boundary that mounts only on viewport proximity or user intent; the follow-up analyzer/build state no longer includes `framer-motion`, `next-auth/middleware`, or `react-dropzone`
- the protected creator resource create/edit routes now also defer the heavy client form bundle behind `next/dynamic` with structural loading shells, and admin create-form lazy loading now renders a matching skeleton while the client chunk hydrates
- the `/resources` discover home now uses a smaller curated server payload: source pools are capped at 6 items per section, rendered server sections/fallback rows are capped at 4 cards, discover collection sources load sequentially, and the category chip loader now uses a short-lived best-effort category fallback instead of waiting indefinitely on a DB read the shell can live without
- Storybook is now available as a design-system-only surface for primitives/components; `npm run storybook:build` and `npm run storybook:smoke` are the verified paths right now because Storybook v10's `dev --smoke-test` flow hit a local CLI port bug in this environment
- Next dev now explicitly allows `127.0.0.1` in `allowedDevOrigins`, which removes a noisy class of local Playwright/HMR cross-origin failures during browser verification
- the CSP header now allowlists `https://va.vercel-scripts.com`, so local/runtime browser verification no longer reports Vercel Analytics / Speed Insights scripts as blocked console errors
- the app now serves a generated `robots.txt` from `src/app/robots.ts` using build-safe public platform config, closing the old local/public 404 without making metadata generation DB-bound
- `/resources/[slug]` stopped reading cookies/session at the page level; ownership, payment-success recovery, and owner-review UI now hydrate from `/api/resources/[id]/viewer-state`
- `/resources/[slug]` detail viewer-state is now split into `scope=base` and `scope=review` so purchase/success/ownership UI does not wait on the owner-review query; the owner-review branch now also uses viewer-scoped session-storage persistence with explicit invalidation on successful review writes so hard refreshes in the same tab do not always restart from an empty review slot
- anonymous `/resources/[slug]` visits now skip the private detail viewer-state API entirely until the lightweight auth viewer confirms the user is signed in
- viewer-state hydration now starts without waiting for NextAuth client-session readiness to settle first
- repeated signed-in ownership reads now use short-lived private `unstable_cache` entries, detail refresh can bypass them after checkout, and detail base viewer-state now also has a short-lived browser cache plus session-storage persistence to smooth remounts, revisits, and hard refreshes in the same tab
- a later follow-up narrowed that detail viewer-state hydration to post-mount reuse only: the purchase rail still reuses the short-lived browser cache after mount, but the initial client render no longer consumes the cached viewer-state snapshot before hydration because that was causing a placeholder-versus-owned/buy-CTA mismatch on the resource-detail purchase rail during fast entry/return flows
- the detail purchase rail now shows a structural "Checking your library…" placeholder while deferred ownership state resolves, preventing buy-CTA flicker for signed-in owners after auth probing moved to idle
- pricing and buy-button CTA components still prime on user intent, but they now also reuse the persisted auth-viewer snapshot immediately when available so returning signed-in users do not always pay the older idle-only account check before CTA decisions settle
- the shared auth-viewer hook now lets callers opt out of cache-hydrated first
  render, and the public navbar uses that stable loading-first path so hard
  refreshes on public pages like `/membership` no longer risk hydrating from a
  stale cached viewer snapshot and replacing server auth placeholders with
  login/register links mid-hydration
- the public `/membership` route kept the same DS-first shell/client split, but
  its latest redesign is now closer to a Linear-style pricing surface: one
  left-aligned heading row, one billing toggle, and three divider-separated
  pricing columns, with the extra compare/trust/FAQ sections removed so the
  route loads directly into plan selection instead of a marketing-heavy stack
- `PriceLabel` now resolves through theme-aware DS text tokens instead of hardcoded dark text, which removed the dark-detail purchase-rail price contrast regression without needing per-page overrides
- category smoke route now matches its actual page intent and is warmed explicitly
- the post-deploy warm script now burst-aligns the hot `/resources`, listing-control, hot resource-detail, hot creator-detail, and category-detail routes to the same 5-VU class used by the smoke suite, and it now reheats the listing-control pair, hot resource-detail, category, creator-detail, and `/resources` home as the final route-class warm steps before k6, with `/resources` home closing that band after manual baseline runs showed it could still go stale behind later creator/category passes; creator public profiles also gained a Redis-backed cross-instance cache layer on top of `unstable_cache`, reducing post-deploy cold-tail variance on `creator_detail_smoke`
- `/creators/[slug]` metadata now uses its own lighter cached metadata reader instead of reusing the full creator public-profile payload, the full creator-profile cache now inlines creator momentum/status-badge fields from the main profile query rather than issuing a second `creatorStat` lookup inside the cached loader, and the internal creator warm path now seeds both metadata and full-profile caches for the hot creator identifiers
- `/creators/[slug]` now also splits creator shell data from the published-resource grid on the server path: the route starts both cache-backed promises together, only blocks the page root on the lighter shell read, and streams the published-resource section behind a structural fallback while the creator warm path primes the new shell/resource caches too
- `/creators/[slug]` and `/categories/[slug]` now render their static public resource lists through a server-led public card shell instead of the generic client `ResourceCard`, trimming marketplace navigation-overlay/prefetch client code from those public grids without changing their visual layout
- `/resources` discover home now also renders the default `Top picks` and `Trending now` rows through the same server-led public card shell, so the hottest anonymous home-path rows avoid hydrating marketplace card overlay/prefetch logic unless the signed-in personalized module replaces them later
- the default discover fallback on `/resources` also no longer mounts `ResourcesViewerStateProvider`; that provider now appears only when the signed-in personalized island actually loads, which removes another viewer-state client boundary from the anonymous home shell
- the discover-home lead subtree on `/resources` now also has a short best-effort timeout guard, joining the existing collections timeout so either discover section can drop out instead of stretching the entire resources-home response when the quick-browse shell is already ready
- the `/resources` browse route no longer feeds hero/content, discover lead/collections, or no-results recovery promises into request-level `trackRequestWork()`: request tracing still records the route shell, but it no longer waits for those deferred Suspense subtrees before the route can stream, which removes an observability-induced blocker from `resources_home_smoke`
- the discover collections source group on `/resources` no longer runs serially on the cold path; `popular`, `newest`, `featured`, and `topCreator` source reads now fan out in parallel before the shared resource-pool lookup, trimming real server latency instead of only raising timeout budgets
- a later follow-up tightened the cold path further: the discover `lead` and `collections` readers now converge behind one shared homepage bundle cache, and `/resources` no longer forces those streamed sections through hard 600ms timeout fallbacks during normal slow-but-healthy requests
- marketplace category sidebar/filter data now uses the same longer homepage-class shared TTL as the hot listing landing caches, reducing category-cache churn on `/resources?category=all&sort=newest` and the other first-page listing routes
- resource-detail warm coverage now matches the measured `/resources/[slug]` route shape more closely: instead of warming the older `getPublicResourcePageData()` compatibility wrapper, the warm path now seeds the shell, metadata, purchase-meta, body, footer, public reviews, and related-resource caches separately so cold detail failures are less likely to come from an unwarmed section cache
- `/resources/[slug]` now also starts the related-resources branch at the page entry alongside the other secondary detail promises instead of waiting for the lower Suspense subtree to execute first, so related suggestions can spend more of the route lifetime actually fetching rather than idling behind later tree work
- another follow-up then collapsed the shell/body/footer/purchase-meta slug reads onto one shared detail bundle cache and removed the hard timeout race from the streamed trust/review/related branches, so local targeted `/resources` + `/resources/[slug]` smoke runs can pass cleanly without emitting the old discover/detail timeout warning class when the requests are merely slow rather than actually failing
- the post-deploy public warm script now reheats `listing_recommended` and `listing_newest` as the final route-class warm step before k6, after creator/category pages have already been warmed, so the two experiment-controlled listing routes that have shown the most frequent fresh-instance tail spikes finish as the freshest warmed surfaces; those warm hits now also send the same `ranking_variant=B` / `ranking_variant=A` cookies that the smoke suite measures, so the route-level warm branch matches the actual recommended/newest runtime path instead of only warming the adjacent query shape
- the same `listing_newest` control arm now also primes a dedicated fixed-key landing cache and clears it from targeted mutation/warm paths, so the measured `sort=newest&page=1&category=all` route no longer depends only on the generic listing cache surface
- the post-deploy warm script now also includes a deploy-only `/resources` stability gate after the usual warm fanout: it probes the same home route in small concurrent bursts, checks the same `900ms` p95 budget as `resources_home_smoke`, and reheats `/resources` before retrying if the home route still shows a cold-tail miss. This keeps the mitigation inside the deploy path rather than relaxing route budgets or changing public runtime behavior just to quiet a post-deploy spike
- the post-deploy perf runner now adds one more deploy-only safety rail for that same noisy route: `resources_home_smoke` can rerun against the same `900ms` budget after a narrow `/resources` reheat if the first pass still catches a fresh-instance tail, but the workflow still fails if the route cannot pass within the capped retry count
- `/categories/[slug]` now starts its marketplace listing read once and streams the hero count plus listing section behind structural `Suspense` fallbacks, so the category hero shell no longer waits on the full category listing payload before first render
- the same category route now renders a static server-led card grid instead of hydrating `ResourceGrid`'s progressive-load/search-context client machinery, trimming client work from the public category listing surface
- `/resources` discover fallback no longer swaps in fake CTA content while data resolves
- discover hero loading now falls back to a neutral reserved stage that keeps the live hero footprint without previewing fake banner content; discover sections still fall back to section/card skeletons that match final geometry
- route-level `/resources/loading` now matches the discover UI more closely instead of showing a stale meta strip or a generic card wall
- creator activation and ranking debug admin reports now use short-lived service-level caching instead of re-running the same read-heavy queries every request
- platform metrics, purchase analytics, and recommendation report admin reads now use a Redis-backed cross-instance cache layer in addition to `unstable_cache`
- several admin analytics/creator pages no longer repeat `requireAdminSession()` inside the page because the admin layout already gates that subtree
- browser/perf verification is now explicitly layered as `core / sentinel / drill-down` instead of assuming one warmed smoke suite can describe every route family: the core post-deploy k6 gate still owns the six public marketplace baselines, a separate report-only sentinel k6 suite now tracks non-default public listing blind spots (`/resources?category=art-creativity`, `/resources?search=worksheet`), and browser smoke now has a matching sentinel Playwright lane for dropdown-origin dashboard navigation, filtered/search listing pages, and admin resource-editor routes

---

## Current High-Value Optimization Areas

### Route-Class Incident Ledger Baseline

- `listing_newest_smoke`
  - main class: cold-instance fanout mismatch on the control/newest listing route
  - current mitigation: warm against `ranking_variant=A` and match the later smoke-VU fanout with a burst of `5`
- `creator_detail_smoke`
  - main class: creator public-profile tails can still appear on fresh instances when the route has to pay both metadata/profile work and the full creator page render under load
  - current mitigation: Redis-backed shared cache plus a stable `unstable_cache` wrapper per slug, lighter cached metadata reads for `/creators/[slug]`, inlined creator-stat fields inside the main profile query, shell/resources cache separation with streamed published-resource rendering, server-led public resource cards for the static published-resource grid, direct `resource.findMany` reads for the published-resource section instead of a nested `user → resources` relation, and burst-aligned warm coverage against the hot creator slug
- `category_listing_smoke`
  - main class: fresh-instance tail on category listing renders under the 5-VU smoke ramp and the route shell used to wait on the full listing payload before returning HTML
  - current mitigation: explicit category warm target with `repeat: 2` and `burst: 5`, plus streamed category shell/listing separation with structural in-page fallbacks, a server-led public card shell for the static category grid, and a dedicated category landing reader that skips the generic marketplace sidebar/filter data path
- `listing_recommended_smoke`
  - main class: the treatment/recommended listing route can still show cold-instance tail latency if warm only touches one worker, even when the control/newest path is already aligned
  - current mitigation: match the smoke route's 5-VU fanout with `repeat: 2` plus `burst: 5`
- `resource_detail_smoke`
  - main class: a single hot detail warm hit can still leave one late fresh instance cold during the later 5-VU smoke ramp even when the detail shell/data caches themselves are aligned
  - current mitigation: keep the detail shell cache narrow, separate metadata from the route-entry shell payload, warm the hot detail route with `repeat: 2` plus `burst: 5`, and reheat that same hot detail route again as one of the final warm steps before k6
- post-deploy warm endpoint layering
  - when `PERFORMANCE_WARM_SECRET` is available, the warm script should call `/api/internal/performance/warm` before the public HTTP fanout pass
  - intent: prime service-level Redis/precomputed listing caches first, then let route-level warming handle page shells, streamed HTML bodies, and image-optimizer hints
  - the workflow should not run a second standalone internal-warm step after route fanout; the final action before the smoke suite should stay the route-level warm pass so `/resources` and listing shells are the freshest warmed surfaces when k6 starts
- `resources_home_smoke`
  - main class: discover-home stream and page shell were still seeing fresh-instance tails after deploy
  - current mitigation: `/resources` still keeps the required warm burst and tail reheat, and the post-deploy warm script now runs a dedicated stability probe on the same route/budget before k6 begins; the main app-level mitigation remains route decomposition itself: quick-browse shell renders immediately while discover `lead` and `collections` stream from separate cached readers instead of waiting on a monolithic discover payload, the default `Top picks` / `Trending now` rows now use a server-led public card shell instead of the generic client card wrappers, both discover subtrees now have short best-effort timeout guards so a cold lead/collections refresh cannot hold the whole response open, and the route no longer tracks those deferred promises as request-level pending work that would otherwise force the response to wait for Suspense children anyway
- `listing_newest_smoke`
  - main class: the control/newest listing can still leave one late fresh instance cold even after the first burst-aligned pass
  - current mitigation: retain `burst: 5`, keep repeated warm passes, reheat the listing route again as one of the last warm steps immediately before k6 begins, prime a dedicated fixed-key landing cache for the hot `sort=newest&page=1&category=all` shape so the warmed smoke route and runtime landing path hit the same Redis surface, keep the shared categories sidebar cache on the longer homepage-class TTL so that route does not cold-miss category/filter data faster than its landing cache, and split the exact result total into a separate cached count key so cold first-page misses do not pay both `findMany` and `COUNT(*)` in the same request
- `listing_recommended_smoke`
  - main class: the recommended listing can still spike when it was warmed successfully earlier in the sequence but is no longer the freshest route-class surface by the time k6 starts
  - current mitigation: retain `burst: 5`, keep repeated warm passes, reheat the listing route again as one of the last warm steps immediately before k6 begins, and treat the all-category first page as its own landing shape with a stronger precomputed cache plus a rows-only activation-ranking query paired with a separately cached total count
- public route request-binding audit (2026-04-08)
  - `/resources`, `/resources/[slug]`, `/creators/[slug]`, and `/categories/[slug]` do not currently read `cookies()`, `headers()`, or server-session state at the page level
  - the remaining perf work should therefore focus on cache reuse, streaming shape, and hydration scope rather than re-removing obvious page-level request binding
  - public creator owner chrome now also follows that rule with a lighter client-side path: the `Edit profile` affordance on `/creators/[slug]` reuses the persisted auth-viewer snapshot eagerly instead of waiting for a fresh viewer fetch after every hard refresh in the same tab
  - signed-in `/resources` return navigation now avoids one extra generic-to-personalized repaint class too: once the personalized discover island chunk has loaded in the current tab, the route can remount that same client section synchronously on later visits instead of replaying the generic server fallback rows while the dynamic import resolves again
- public route resilience audit (2026-04-09)
  - `/resources` listing/search-recovery/discover controls now treat transient Prisma infrastructure failures as fail-soft and surface an in-page unavailable state or omit the deferred section instead of throwing the entire route into `resources/error.tsx`
  - the route owner now also wraps the async catalog-controls navbar subtree in the same fail-soft policy, so a transient pool error during `/resources` client return navigation cannot bypass the content-level guard and trip the full route error boundary
  - the same `/resources` return-navigation path now trims route-family prefetch noise aggressively: visible discover/detail links that target sibling `/resources` listings or `/resources/[slug]` pages no longer auto-prefetch on viewport/default mount, which removes the burst of abandoned RSC requests and the related risk of caching a transient infra failure before the user actually navigates
  - `/resources/[slug]` and `/creators/[slug]` now also degrade more gracefully on transient infrastructure failures: metadata falls back to safe defaults, critical shell reads can render dedicated temporary-unavailable states, and non-critical creator-resource sections can resolve to empty results instead of tearing down the page shell

## Protected Dashboard Dev-Compile Notes

- 2026-04-30: The next protected-dashboard perf slice narrowed `/dashboard/library` without touching `DashboardSections.tsx`. The route no longer renders the generic client `ResourceCard` stack from `src/components/resources/ResourceCard.tsx` for owned-library tiles; it now uses a route-local server-led `DashboardLibraryResourceCard` with plain route actions (`download / preview / open`) and a static purchased-time label. That removes `usePathname`, reveal-image state, marketplace intent-navigation helpers, and the interactive library-footer client island from the cold library route graph. In the current local dev environment, a clean-start shell probe to `/dashboard/library` dropped from the earlier `35.0s` server-log baseline to roughly `15.0s` total on the new probe path, which is not a perfect authenticated browser 1:1 measurement but is strong enough to confirm that the route-level product-card graph was a real hotspot after auth-session scope had already been narrowed.
- 2026-04-30: The next shared-shell perf slice narrowed `DashboardAppNavigation.tsx` itself. The dashboard topbar no longer statically imports the authenticated/public account-menu graph or `next-auth/react`; that logic now lives in a lazy `DashboardAccountMenu` module with a non-ready fallback trigger, so the shared dashboard shell stops dragging account-menu and sign-out client code into every cold route compile by default. The clean-start `/dashboard/library` shell probe stayed in the same improved band (`~14.4s` vs the prior `~15.0s` route-only probe), which means this slice is more about keeping shared-shell graph growth in check than claiming a second dramatic win, but route proof (`tests/e2e/navigation-sentinels.spec.ts`) confirms the lazy menu still preserves dashboard/resources/admin navigation contracts.
- verification coverage policy (2026-04-09)
  - the core k6 gate remains `/resources`, recommended/newest first-page listings, hot resource detail, hot creator detail, and hot category listing
- sentinel coverage is now intentionally broader but lighter-weight: browser smoke owns auth-shell, dropdown, filtered-listing, and editor-route correctness, while post-deploy sentinel perf keeps extra public listing shapes visible without turning every new route into a hard p95 gate on day one
- the dashboard avatar-menu sentinel now targets explicit dashboard menu data attributes and waits for a hydration-ready signal on the trigger before opening the menu, so CI failures in that lane reflect a real menu contract break instead of a race against a visible-but-not-yet-interactive client control
- the dashboard navigation sentinels now also assert that visible `[data-loading-scope="dashboard-group"]` overlays are gone after avatar/account-menu navigations, so probes cannot pass while a full-screen dashboard skeleton is still visually covering the destination route
- hot first-page marketplace search listings now have a bounded per-instance cache layer (no Redis) so repeated searches such as `/resources?search=worksheet` can reuse the ranked search listing render path during drills/sentinels without introducing long-lived shared stale search-result caches
- hot first-page all-category recommended/newest listings now also use a stronger shared landing-cache tier (`listingLanding`) and a dedicated cached total-count key, so warm coverage can keep the route payload and its exact total aligned without forcing every cold first-page miss to recompute both the rows and the count together
- family drill-down should be manual or failure-triggered, not always-on: `listing-drilldown` now exists to cluster the public listing family (`/resources`, recommended/newest, category page, non-default category listing, and search listing) when the core/sentinel layers signal instability
- dashboard-shell geometry is now standardized across user and creator pages: route headers use one shared contract, and dashboard routes now keep the same `space-y-8` shell rhythm across overview, library, downloads, purchases, settings, and membership so loading handoff is less route-specific
- dashboard user fallbacks are now state-agnostic where sharp state branches used to create the worst shifts: membership no longer loads as a fake active-plan hero before data resolves, and library no longer reserves a recovery banner that only appears on checkout-return paths
- the dashboard result-section pass now goes one step further on the highest-shift branches: `/dashboard/membership` chooses a free-vs-active results skeleton from the signed token snapshot, `/dashboard/library?payment=success` reserves the purchase-recovery geometry only when the checkout-return flow is actually present, and creator overview fallbacks now reserve banner/recent-sales/balance/table sections in the same order as the final surface instead of collapsing into a generic card stack
- the dashboard standardization pass now routes both runtime pages and loading previews through the same `DashboardPageShell` contract, so the outer width/rhythm/route-ready shell is no longer reimplemented separately in learner routes, creator workspace, and their skeleton files
- the dashboard loading audit now treats single-owner body loading as the target contract: the family layout remains shell-only, but route-level `loading.tsx` should own the visible route body whenever a page needs a substantive loading state. In-page `Suspense` fallbacks are now reserved for secondary subsections instead of duplicating the main body handoff
- dashboard sidebar rail, mobile drawer, creator callout, and avatar account-menu links now use the repo-owned `IntentPrefetchLink` helper instead of bare `next/link`, which adds viewport/interaction-driven `router.prefetch()` to the canonical learner/account/creator destinations and reduces the amount of route warmup that only begins after a dashboard click
- the Phase 1 cleanup then split the old dashboard user skeleton monolith into `dashboard-loading-contract.tsx` plus route-family files under `src/components/skeletons/dashboard-user/*`, with `DashboardUserRouteSkeletons.tsx` as the explicit dashboard barrel. That refactor does not change runtime semantics by itself, but it removes the main source of repeat regressions where downloads/purchases/subscription edits kept reintroducing each other's loading bugs through one shared file
- historical note: the older dashboard-lite pass established the shell-only vs route-body loading ownership rule that the current canonical dashboard family still follows, even though the `(dashboard-lite)` route group itself has now been retired
- the same ownership rule now applies to canonical `/dashboard/library`, `/dashboard/downloads`, and `/dashboard/purchases`: those pages no longer keep a page-level results `Suspense` fallback for the main body, so route `loading.tsx` is the only loading owner and the final body appears in one pass once route data is ready
- dashboard ready detection now only counts stability inside the destination `main` surface; visible full-screen overlays elsewhere in the document no longer satisfy the clear condition on their own, which reduces false-ready clears during dashboard entry handoff
- historical note: the older Phase 3/4 creator pass established the creator protected shell ownership rule before the hard cut retired the old `(dashboard)` tree; the active canonical dashboard creator subtree still follows that same shell/body loading split
- the creator editor routes then removed one more first-entry overlap source: `resources/new` and `resources/[id]` no longer hand the same `CreatorResourceFormLoadingShell` to both `next/dynamic` chunk loading and the page-level `Suspense` boundary. The editor route shell still comes from the route-level `loading.tsx`, but the form surface now streams through a single fallback owner instead of briefly stacking identical form shells on top of each other during the first cold visit
- the creator workspace phase-3 follow-up now pushes that rule through the rest of the protected route bodies: analytics, resources, sales, profile, and both resource editor pages no longer keep a second page-level main-body `Suspense` fallback at all, so the protected creator routes now behave like the hardened admin routes where `loading.tsx` owns the visible body skeleton and the final page body renders in one pass once route data is ready
- the protected creator gate also dropped the duplicated `requireSession()` hop and now reuses the token-first creator user helper before loading `getCreatorAccessState()`, trimming one auth read from the creator layout suspense path even though the actual creator-access query still remains
- dashboard browser probes now also benefit from the same-route guardrails that the runtime shell uses: clicking an already-active sidebar item or avatar-menu link no longer restarts dashboard navigation state, which reduces false “hang” behavior caused by overlay/progress re-entry without a real route change
- the same Phase 4 pass now adds an explicit overlap guard to repo-owned verification: `scripts/browser-probe-local.ts` samples visible `data-loading-scope` values during refresh, avatar/account-menu, and creator workspace flows, and `tests/e2e/creator-workspace.spec.ts` asserts the same invariant in Playwright. Both layers fail if more than one dashboard/creator full-shell scope is visible at the same time, so “full-shell-on-full-shell” regressions are caught automatically instead of being inferred manually from screenshots
- first-entry dashboard handoff now has a second verification class beyond scope overlap: cold-entry browser probes for `/dashboard/settings`, `/dashboard/membership`, and `/dashboard/creator` navigate from a non-dashboard page and fail if family/route shell scopes are still visible after the destination route-ready marker appears. This catches the “it only happens the first time I enter the page” class that refresh-only probes can miss
- the same cold-entry verification now proved the concrete wrong-level fallback rule for dashboard routes: App Router segment `loading.tsx` is rendered inside the resolved route-family layout slot, so it must never render the full `DashboardGroupLoadingShell`. The current hardened baseline is therefore `layout.tsx` owns the full dashboard chrome fallback, while segment and route `loading.tsx` files own content/body skeletons only
- `settings` and `membership` now follow a shell-first learner-account rule instead of the older one-pass body lock: route `loading.tsx` still owns the cold-entry full-page fallback, but once the route shell is mounted the main settings sections / membership results body stream behind one in-page structural `Suspense` fallback that matches the final geometry rather than reverting to a second generic full-body shell
- the returning creator dashboard overview route now also follows that rule: the overview page no longer renders its large results skeleton behind a `Suspense` boundary on first entry, so the route-level creator shell owns the loading state instead of handing off from shell to another full overview skeleton once the page has already mounted
- `creator/apply` now follows that same model for non-creators: the route-level loading file owns the visible onboarding body skeleton, the page no longer streams the main status/form panel through `CreatorApplyPanelSkeleton`, and only the rejected-feedback note stays behind a small nested `Suspense` boundary when that subsection is present
- repo-owned dashboard verification now covers that non-creator path explicitly: `tests/e2e/helpers/auth.ts` provides a regular-user login fixture, `tests/e2e/creator-workspace.spec.ts` asserts `/dashboard/creator/apply` clears overlays without shell stacking for non-creators, and `scripts/browser-probe-local.ts` adds a `creator-apply-cold-entry` scenario so cold first-entry regressions are measurable
- static enforcement now backs up the runtime probes: `scripts/check-app-skeletons.mjs` still blocks inline app-layer skeleton declarations, and the remaining dashboard loading checks now prevent route pages from reintroducing disallowed main-body `Suspense` / `next/dynamic` loading owners or old full-shell dashboard fallback patterns
- dashboard skeleton owners now import page-shell/header primitives directly
  from `src/components/layout/dashboard/*`, and the last `dashboard-v2`
  component filenames are gone from the active loading path, trimming another
  layer of compatibility indirection from dashboard-first entry
- creator resource timing now has a lighter hot path too: `findCreatorCategories()` is cached at the service layer for canonical dashboard creator inventory/editor flows, and `getCreatorResourceManagementPageForWorkspace()` now overlaps category-taxonomy warming with the status-summary read before it computes filtered counts and paged rows, trimming one avoidable serial step from `/dashboard/creator/resources`
- rollback baseline note (2026-04-14): the active dashboard runtime perf baseline after the route-by-route rollback is intentionally narrow: keep `Phase 3A` nav prefetch uplift and `Phase 3B` creator/resources timing cleanup, but treat later summary-first runtime splits for `library`, `downloads`, `creator/workspace`, `creator/sales`, and `creator/payouts` as reverted experiments rather than current behavior
- warm verification note (2026-04-14): `tests/e2e/creator-workspace.spec.ts` passed `8/8` on a warm local dev server after that rollback baseline was restored, so current evidence points to no contract regression on the reverted route family and the remaining instability risk is dev-only cold-start flake rather than a known dashboard loading mismatch
- dashboard-family audit follow-up (2026-04-09)
  - historical note: the old `(dashboard)` shell no longer exists after the hard cut, but the token-first shell identity rule it introduced still applies to canonical dashboard account and creator routes
  - dashboard handoff now clears from one shared navigation-state path instead of overlapping local clear loops; the old `DashboardEntryNavigationOverlay` / `DashboardGroupNavigationOverlay` split is retained here only as historical context, not as an active runtime ownership map
  - this removes the main remaining “dashboard in dashboard” class where cross-group dashboard entry briefly stacked multiple full-screen dashboard shells on top of one another even after route-ready markers existed
- management long-tail shell audit (2026-04-09)
  - `/admin/audit` now follows the same shell-first pattern as the other hardened admin routes: filter/header chrome renders first, while the heavy audit table and pagination stream behind an in-page `Suspense` fallback
  - `/dashboard/creator/apply` now keeps the route-level loading shell as the only visible body owner and limits deferred loading to the rejected-feedback subsection, which removes the extra first-entry handoff between a creator-program shell and a second status/form panel skeleton
  - `/admin/reviews` now fails soft for transient Prisma infrastructure issues, and the admin long-tail routes that do not need session data themselves (`/admin/resources/trash`, `/admin/resources/[id]/versions`, `/admin/resources/bulk`) no longer repeat `requireAdminSession()` on top of the admin layout gate
  - `/admin/resources/new` and `/admin/resources/[id]` now render the editor header immediately and stream the heavy create/edit form data behind a form-only fallback, instead of blocking the whole route on admin reference data and a duplicated admin-session read
  - historical note: the previous `/dashboard/resources` compatibility redirect fast-path was retired during the hard cut; creator apply/resources routing now enters through canonical dashboard creator routes directly
  - `/admin/analytics/purchases` and `/admin/analytics/ranking-experiment` no longer repeat `requireAdminSession()` at the page level; they now rely on the admin layout gate like the other hardened admin routes, trimming an extra auth hop from those report paths

### 1. Cold-tail variance on warmed routes

- Smoke failures can still happen when a route is not warmed or is warmed against the wrong query path.
- Keep post-deploy warm targets aligned with the actual perf routes and route intent.
- Some deploys still show first-hit or fresh-instance instability on `/resources`; the warm workflow now intentionally gives that route both the main burst-aligned warm pass and a dedicated tail reheat before k6 begins, instead of loosening the budget blindly.

### 2. Discover feed cold path

- `/resources` is still a data-heavy route with multiple cached sections and post-hydration personalization.
- The route-level auth/cookie bottleneck was removed, and owned-state now hydrates ahead of recommendation/discover personalization.
- Signed-in personalization still depends on a private client fetch after hydration, but it no longer blocks owned badges and repeat navigations now reuse a short-lived private cache.

### 2.5 Auth-aware public chrome

- Global NextAuth client-session hydration was removed from the root.
- Remaining public auth-aware UI now depends on a lightweight auth-viewer request, which is smaller but still a client-side personalization step.

### 2.75 Global client-JS scope

- Bundle/Lighthouse baseline work on 2026-04-07 still points to unused JavaScript / main-thread blocking as the clearest remaining client-side signal on `/resources` and `/resources/[slug]`.
- Route-specific transition overlays now mount only inside their matching route layouts, but further client-boundary and lazy-loading work is still a likely high-value path before deeper query refactors.

### 3. Search indexing

- Search still relies on `contains` / `ILIKE`-style matching.
- The major missing trigram indexes for marketplace/admin search are now covered; remaining search work would mean changing the search strategy itself, not just adding obvious indexes.
- The search strategy itself is now more usable than before, but it is still Postgres-backed relevance scoring, not a dedicated full-text/vector engine.

### 4. First query on resource detail pages

- Resource detail shell/body/footer/purchase decomposition is in place.
- The first `getResourceBySlug()` path is still the main blocking query to inspect if detail TTFB regresses again, but it now serves a narrower shell projection while metadata and description live in separate cached reads.
- The old route-level optional session bottleneck was removed, and ownership/success state no longer waits on the owner-review query.
- Signed-in detail personalization still depends on client viewer-state fetches after hydration, but owner review is now a secondary fetch instead of part of the purchase-critical path.
- Anonymous detail visits no longer spend a client request on ownership state they cannot have.

### 5. Recommendation / related tail latency

- Personalized discover sections and related-content sections can still lag behind the shell.
- Fallbacks are now more consistent with final content, but these sections are still natural perf hotspots.

---

## Current Known Ops / Perf Notes

1. Production/build path still warns that `XENDIT_SECRET_KEY` is a test key
2. Perf thresholds should stay aligned with what warm-cache actually warms
3. Search indexing is no longer the obvious gap; remaining work is query-plan tuning and deciding whether Postgres-backed relevance is still enough long-term
4. Future regressions should be judged against the warmed perf workflow, not older cold-path assumptions
5. LHCI is still configured against `npm run dev`, so local Lighthouse numbers remain useful for regression detection but not as production-grade LCP truth
6. The stronger LHCI gate is meant to catch obvious regressions early from the current repo baseline, not to replace warmed post-deploy k6 verification on production URLs or to act as an aspirational production budget by itself
7. Operational perf review order is now explicit: check the warmed post-deploy perf summary first, then Speed Insights, then runtime logs if the route still needs deeper investigation
8. Close-out reporting for non-trivial runtime work now also requires `Verification`, `Knowledge triage`, and `Residual risk`; a green workflow without log review is not a complete verification summary
9. A post-deploy run where `Warm Public Cache` passes but perf verification fails should be treated as its own recurring failure class: the workflow warmed successfully, but one or more measured route shapes are still unstable, mismatched with the warm target, or vulnerable to fresh-instance variance; when the failing smoke route ramps to 5 VUs, first check whether the warm burst still undershoots that fanout before loosening budgets
10. Hard-refresh reports that "dashboard briefly became another page" have recently resolved to wrong-level fallback hierarchy, not an actual router jump; check `src/app/loading.tsx` and route-family loading boundaries before treating that class as a route bug
11. Admin analytics long-tail pages are now part of the shell-first hardening sweep too; when those routes regress again, inspect whether filter data and report-body data were accidentally re-coupled into one awaited query path before chasing lower-level DB tuning
12. Admin creator moderation now also fail-softs for transient Prisma/pool failures; if `/admin/creators` starts collapsing again, inspect whether the creator-application reader regained a hard route-level throw for infrastructure errors before assuming the application query itself regressed
13. Admin resource long-tail routes now include shell-first hardening too; when `/admin/resources/trash` or `/admin/resources/[id]/versions` regress, check whether their result tables/cards were accidentally pulled back into the page-level await path instead of staying behind the route-specific results fallback
14. Onboarding routes should use role fast-paths before falling back to creator-access reads; `/dashboard/creator/apply` short-circuits for clearly admitted roles, so if that path regresses again inspect whether the fast-path was removed before blaming creator-access cache latency
15. Resource detail is now hardened as a critical-shell route rather than a full shell-first split: `/resources/[slug]` starts slug-based secondary readers in parallel with the primary resource fetch and enforces timeout-based fail-soft on purchase/trust/reviews/related branches, so regressions there should be investigated as section-level timeout/fallback behavior before assuming the critical shell query itself regressed
16. Reviews and creator moderation now expose lighter summary branches before their full tables; if those pages feel heavy again, inspect whether the shared moderation promise got pulled back into one blocking render path instead of feeding separate summary/results suspense sections
17. The ranking debug route now splits the category-filter chrome from the report body more aggressively; if the page regresses, check whether the filter-data reader got re-coupled to the full report path before tuning the report query itself
18. Public account-menu navigation into dashboard routes now depends on explicit client prefetch/warmup instead of relying on hidden dropdown links to prefetch themselves; if `/dashboard/settings`, `/dashboard/library`, or `/dashboard/purchases` feel stuck on the full dashboard skeleton specifically after a menu click from `/resources`, inspect the navbar/avatar prefetch hooks before blaming the route body
19. Dashboard geometry proof is now branch-aware for the user-facing dashboard routes; when `/dashboard/library` or `/dashboard/membership` look wrong in proof, first verify the selected preview branch (`empty` vs `populated`, `free` vs `active`) before treating the diff as a layout regression
20. Protected dashboard user routes also have a dev-only compile-scope risk: `library`, `downloads`, and `purchases` originally imported one `DashboardSections.tsx` file that had grown past `5k` lines / `188 KB`. The first compile-scope reduction slice now moves those three routes onto route-local modules and a shared `DashboardRouteIntro`, cutting the monolith down to `4147` lines / `153 KB`. If dashboard dev compilation regresses again, inspect whether route-local dashboard pages have drifted back to importing the monolithic sections file instead of their narrower modules before blaming machine memory pressure alone.
21. The same compile-scope risk also affected creator workspace flows. `creator/(workspace)` and the loading shells shared by `creator/storefront` / `creator/settings` now import a route-local workspace module, while `DashboardCreatorStats` is its own shared helper so workspace and analytics do not have to reach back through the monolith for the same stat-card contract. After that second slice the monolith dropped again to `3405` lines; further dev-latency work should keep peeling route families off this file incrementally instead of attempting a one-shot rewrite.
22. The next compile-scope reduction slice now extracts `/dashboard` overview into its own route-local module too. `DashboardSections.tsx` dropped again from `3405` lines to `2997`, and the warm redirect probes for `/dashboard` and `/dashboard/library` stay in the low-millisecond range after a clean restart. The first cold dev compile for authenticated `/dashboard` is still expensive though (`GET /dashboard 200 in 33.7s`, with ~29.6s in Next.js compile time), so the remaining dev-latency work should target other heavy route families still importing the monolith, especially creator resources/editor and membership/settings, before assuming the file-size problem is fully solved.
23. The next incremental slice now extracts the whole `creator resources/editor` family too: `/dashboard/creator/resources`, `/dashboard/creator/resources/new`, and `/dashboard/creator/resources/[id]` plus their loading shells now render through one route-local module instead of importing `DashboardSections.tsx`. That cut the monolith again from `2997` lines to `2258`, and the remaining dashboard/admin route imports to the monolith dropped from `17` to `11`. After a clean restart, the first cold `/dashboard` compile improved from `33.7s` to `24.3s` (about `21.1s` inside Next compile time), while warm protected redirects for `/dashboard` and `/dashboard/creator/resources` stayed in the low-millisecond range. Remaining compile-scope work should now focus on `membership/settings` or the other creator/admin families still importing the monolith.
24. The next compile-scope slice extracts `membership` plus the streamed `settings` route out of the monolith as well. `DashboardSections.tsx` drops again from `2258` lines to `1684`, and the remaining monolith route-import count falls from `11` to `8`. The interesting part is what did *not* improve cleanly: after a clean restart the first cold `/dashboard` request still logged `36.3s`, and the dev server then spent another `24.1s` compiling `/api/auth/[...nextauth]` for `/api/auth/session`. Warm protected redirects for `/dashboard/membership` and `/dashboard/settings` settled back to low-millisecond timings, so the compile-scope reduction is still structurally valid, but the dominant cold-start bottleneck is no longer just the dashboard monolith. Future perf work should inspect auth/session compile cost or other shared dev graph hot paths before assuming more route splitting alone will keep shrinking the first-hit latency.
25. The next perf slice narrowed auth-session scope instead of splitting more routes. `SessionProvider` no longer wraps the whole app tree; it now mounts only around the `ProfileSettings` subtree that actually calls `useSession()`. After a clean restart, dashboard entry no longer auto-compiled `/api/auth/[...nextauth]` as part of the first dashboard hit: `/dashboard` warmed back down to ~`0.024s` on the second request, and `/api/auth/session` compiled only when hit explicitly, taking about `4.2s` instead of the earlier `24.1s`. The remaining cold `/dashboard/library` compile (`35.0s`) shows there is still real dashboard route work left, but auth-session is no longer the forced global follow-up request for every route under the root providers shell.
26. The next creator-route extraction slice now removes the last live creator protected pages from the dashboard monolith too. `/dashboard/creator/analytics`, `/dashboard/creator/sales`, `/dashboard/creator/payouts`, and `/dashboard/creator/profile` plus their loading shells now import one route-local `DashboardCreatorProtectedRoutes` module, and `DashboardSections.tsx` drops again from `1684` lines to `531` with no remaining live route imports from `src/app/*` back into that file. The current clean-restart truth is again mixed but useful: warm probes for the protected creator redirects settle at `~0.011–0.036s`, but the first local cold request is now dominated by shared startup work instead (`/api/internal/ready` compiled in ~`19.2s`, then `/dashboard/library` logged ~`31.5s`). Treat that as evidence that the monolith is no longer the main live-route graph for creator pages; the next perf work should inspect shared dashboard shell/ready-path graphs or other route-local client islands rather than assuming more `DashboardSections.tsx` slicing alone will explain the remaining cold-start cost.
27. The next shared-shell slice narrows the dashboard topbar endcap itself. `DashboardAppNavigation.tsx` no longer statically owns the notifications dropdown or the account-menu/auth-client graph; those now live behind a lazy `DashboardTopbarActions` chunk, leaving the cold topbar path centered on the visible menu button + search field instead of dashboard-optional dropdown content. This slice also produced a better local proof shape than earlier shell-only probes: using the in-app browser's authenticated session, a clean-restart navigation to `/dashboard/library` completed in about `8.2s`, a warm reload on the same route settled around `2.17s`, and the dev server logged the cold route compile at `6.6s` followed by an `851ms` warm hit. That does not mean dashboard cold-start is solved, but it does move the next hotspot away from the topbar endcap and toward the remaining shared navigation/search shell or route-local app-code path.
28. The next shared-shell slice removes the mobile drawer graph from the default desktop cold path too. `DashboardAppNavigation.tsx` no longer statically imports `@radix-ui/react-dialog` or the mobile drawer subtree; desktop sidebar content/search helpers moved into `DashboardSidebarContent`, and the actual mobile drawer now lives behind a lazy `DashboardMobileNavigation` chunk that only mounts after the user opens the menu. After a clean restart, the first server log for `/dashboard/library` improved to `17.2s` (roughly `14.9s` Next compile + `1.7s` application code) and the authenticated in-app browser warm reload on the same route stayed around `1.8s`. This is another real shared-shell win, but it also confirms the remaining cold-start cost now lives in the still-shared navigation/search shell or route-local app graph more than in dashboard-only account/mobile-menu chrome.
29. The next shared-shell slice narrowed the dashboard topbar search itself. The visible search control on dashboard routes now hydrates through a lazy `DashboardTopbarSearch` island with a structural static fallback shell, instead of forcing the `SearchInput` primitive, router/search-param hooks, and search-submit/clear logic into the default cold dashboard shell path. After a clean restart, the currently authenticated in-app browser measured `/dashboard/library` at about `5.2s` for the first navigation and about `1.47s` for a warm reload, while the dev server logged the cold route at `4.1s` (`2.6s` Next compile + `1.1s` application code) and the warm follow-up at `277ms`. That makes the topbar search graph a confirmed residual hotspot class that is now materially smaller; the next cold-start work should look beyond the search box itself toward the remaining shared dashboard shell path or route-local app graphs.
30. The next route-local perf slice narrowed the creator workspace family rather than the shared shell. `DashboardCreatorWorkspaceRoute.tsx` no longer exports or co-locate the large loading/skeleton tree for the `(workspace)`, `storefront`, and `settings` loading routes; those loading surfaces now live in a dedicated `DashboardCreatorWorkspaceLoading` module, so the final `/dashboard/creator` route graph no longer compiles its own fallback geometry from the same file by default. In the current noisy local-dev run, one cold `/dashboard/creator` compile event landed around `8.5s` (`7.5s` Next compile + `940ms` application code) instead of the earlier `~35.5s` worst-case snapshot from the same residual track. Treat that as a real route-graph reduction signal, but not as a final steady-state budget: the local dev environment still shows heavy auth/public compile noise around the same run, so the next perf work should keep separating route-local dashboard/creator graphs from environment-driven noise instead of assuming one number is now canonical.
31. The next creator-route slice narrowed the protected creator family itself instead of the dashboard shell. `/dashboard/creator/analytics`, `/dashboard/creator/sales`, `/dashboard/creator/payouts`, and `/dashboard/creator/profile` no longer import one `1177`-line `DashboardCreatorProtectedRoutes.tsx` module; each route now points at its own route-local module (`DashboardCreatorAnalyticsRoute`, `DashboardCreatorEarningsRoute`, `DashboardCreatorProfileRoute`), while the old shared file shrinks to a `13`-line compatibility barrel and the small shared empty-state helper moves into its own module. The current clean-restart dev log is still noisy, but it proves the structural split happened: the authenticated runtime walk compiled those creator routes as separate graph events (`/dashboard/creator/analytics` ~`33.9s`, `/sales` ~`7.2s`, `/payouts` ~`3.8s`, `/profile` ~`10.3s`) instead of one reused protected-family module hit, and the existing creator shell regression spec still passed across all four routes plus settings. Treat that as evidence that the remaining residual cost has shifted further toward per-route creator app code or shared startup noise, not the protected-family wrapper file itself.
32. The next creator-route slice narrowed the resources family too. `/dashboard/creator/resources`, `/dashboard/creator/resources/new`, and `/dashboard/creator/resources/[id]` no longer point at one `802`-line `DashboardCreatorResourcesRoute.tsx` module that mixed inventory, editor, and both loading trees; route pages now import `DashboardCreatorResourceInventoryRoute` or `DashboardCreatorResourceEditorRoute` directly, and the old shared file shrinks to an `8`-line compatibility barrel. Clean-restart runtime proof stayed on the existing creator-shell regression test and still passed across resources, editor, profile, analytics, sales, payouts, and settings. The dev log for that proof now shows inventory and editor compiling as separate route-local graph events (`/dashboard/creator/resources` ~`11.8s`, `/dashboard/creator/resources/new` ~`9.7s`, then later a fresh editor-only compile around `3.9s`) rather than one shared resources-family compile. Treat that as another real route-graph reduction signal, while acknowledging that the absolute local-dev numbers are still noisy because `next dev --webpack`, favicon/public compiles, and Fast Refresh full reloads are still part of the same session.
33. The next shared-startup slice now fast-paths creator access for `ADMIN` and `INSTRUCTOR` instead of reading creator-access context on every dashboard shell or creator-route entry. `DashboardAppShell`, `/api/auth/viewer`, and `requireCreatorDashboardAccess()` all now reuse one role-scoped synthetic creator-access state for those roles and only fall back to `getCreatorAccessState()` for learner-like accounts that actually need the DB-backed eligibility check. Runtime proof stayed on existing creator-shell + creator-nav regression tests, and the clean-restart local dev log showed the intended effect on the viewer path: after creator login, `/api/auth/viewer` returned in about `27ms` total (`20ms` Next + `7ms` application code) while sidebar/account-menu creator links still rendered correctly. Treat that as a startup-path reduction for creator-capable roles, not as proof that learner startup or global cold compile is solved.
34. The next learner-facing route slice narrows `dashboard/library` app code itself. `DashboardLibraryRoute.tsx` no longer imports the client `SearchInput` primitive or marketplace-intent navigation wrappers just to render its toolbar and empty/recovery links; the toolbar search is now a server-safe `Input` + local adornment wrapper, and the empty/recovery CTAs use plain `Link` because they do not need the heavy intent-prefetch/state graph in this route. Search parity proof stayed green after the swap, and the same clean-start local run that first exercised the new library route shape logged `/dashboard/library` at about `2.9s` cold and `203ms` warm. Treat that as a route-local graph reduction for the learner library surface, while keeping in mind that the wider dashboard cold-start problem still depends on shared startup noise and webpack dev behavior outside this file.
35. The next learner-facing route slice narrows `dashboard/home` app code too. `DashboardHomeRoute.tsx` no longer pulls `ResourceIntentLink`, `next/image`, or the image-optimizer bypass helper into the continue-learning cards; those tiles now render through plain `Link` plus a lazy native `<img>` because the dashboard overview does not need marketplace-intent navigation state or Next image optimization just to reopen an owned resource. Existing dashboard route-sentinel proof stayed green, and the clean-restart local dev log from that authenticated runtime walk brought the first `/dashboard` hit down to about `10.4s` total (`8.9s` Next compile + `466ms` proxy + `1.1s` application code) before subsequent warm hits settled under a second. Treat that as another real route-local graph reduction for the learner dashboard shell, while recognizing that the remaining residual cost is now more likely to live in shared learner/public startup noise or other per-route app code than in the already-lightened home/library surfaces.
36. The next startup slice removes one public-shell anti-pattern rather than another route graph. `Navbar.tsx` no longer opts out of cached/persisted auth-viewer hydration on every mount; the shared public navbar now uses the default `useAuthViewer()` path, so a same-session public route can reuse cached viewer state instead of forcing a guaranteed cold empty navbar state plus a fresh `/api/auth/viewer` fetch by policy. Runtime proof stayed green on the signed-in public membership navbar and the public `/resources` → `/dashboard/library` handoff. The clean-restart log still shows `api/auth/viewer` and public resources viewer-state requests as residual startup noise, so the next follow-up should target duplicate viewer/public-state requesters rather than the navbar cache policy itself.
37. The next startup slice narrows the public resources viewer-state graph itself. `ResourcesPageContent` now mounts one `ResourcesViewerStateProvider` around the live listing surface, and `ViewerAwareResourceGrid` consumes owned ids from that shared context instead of spinning up its own `useAuthViewer()` + `useFetchJson("/api/resources/viewer-state?scope=base")` hydrator beside the spotlight card. Runtime handoff proof from `/resources` → `/dashboard/library` stayed green, and the clean-restart log now shows the authenticated public walk issuing one `scope=base` viewer-state request before the route transitions to dashboard, instead of separate listing-card and grid requesters competing for the same owned-state payload. The remaining startup noise on that path is now dominated by `scope=discover` personalization and the shared auth-viewer request rather than duplicate base owned-state readers.
38. The last shared auth-viewer slice that still produced a clean win now seeds viewer cache during credentials sign-in itself. The credentials login page primes `authViewer` immediately after `signIn("credentials", { redirect: false })` succeeds and before it pushes the browser to the protected destination. A browser-automation proof against `/auth/login?next=/dashboard/library` confirmed the intended behavior: the login flow reached `/dashboard/library`, `sessionStorage["krukraft.authViewer"]` was already populated on arrival, and only one `/api/auth/viewer` request was issued during the whole transition. The clean-restart dev log for the same flow now compiles `/api/auth/viewer` during login and then reaches `/dashboard/library` with roughly `703ms` of application code on the first route hit, instead of forcing the destination route to discover and seed viewer state from scratch.

---

## What Is No Longer the Main Source of Truth

Older assumptions that should not be treated as current truth:

- “Build runs migrations” — false now
- “Platform settings DB lookup warns during build” — fixed
- “Resource detail is monolithic and blocks on session/trust/reviews” — largely fixed
- “Every public preview image should bypass `/_next/image`” — false now

---

*Refreshed against the repo state on 2026-04-05.*
