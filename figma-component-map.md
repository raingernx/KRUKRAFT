# Krukraft Figma Component Map

Repo-owned manual mapping registry between the live Figma design-system file and
the codebase.

What this file owns:
- the live Figma page/node-to-code registry
- mapping status per shared component or exemplar
- registry maintenance rules

What this file does not own:
- the code-side DS inventory or ownership rules
- token mirroring guidance
- Figma reconstruction or fidelity workflow guidance

Use this file when Code Connect is unavailable or when working on `Professional`
plan handoff workflows. For DS ownership and handoff rules, use
`src/design-system/README.md` and `design-system.md`.

If this file conflicts with code, the code wins. If this file conflicts with the
live Figma canvas, update this file in the same work session.

Validation:

- Run `npm run figma-map:check` after adding or renaming DS component files or
  reusable Figma library components.
- Run `npm run tokens:audit` when a DS token file, export surface, or token
  inventory doc changes.
- The check currently enforces row coverage for every `.tsx` file under:
  - `src/design-system/primitives`
  - `src/design-system/components`

## Live File

- Figma file: [Krukraft Theme Lab Source-of-Truth](https://www.figma.com/design/koZEgVUfQhNEQmXISNQx56)
- File key: `koZEgVUfQhNEQmXISNQx56`
- Workspace status: moved into the shared Team project, not kept in personal
  Drafts.
- Canonical scope today: `DS Foundations` + `DS Primitives` +
  `DS Components` + `DS Parking / Legacy` + `Foundation Review`
  (reorganized on `2026-04-30`)
- Migration note: the canonical file is now foundation-first. Until the
  inventory-diff pass remaps every row, some page names/node IDs below should be
  treated as legacy placeholders rather than verified live-node mappings in the
  new canonical file.

## Canonical Page Audit

Detailed file audit against the live canonical file `koZEgVUfQhNEQmXISNQx56`
was refreshed through the 2026-04-30 page-role split. Use this section as the current page/section
baseline when repo docs and older node claims disagree.

### Page Inventory

| Page | Page node | Top-level coverage | Binding posture | Notes |
| --- | --- | --- | --- | --- |
| `DS Foundations` | `13:20` | 5 top-level frames spanning `Typography`, `Color Primitives`, and `Spacing + Radius` only | 2026-04-30 page-role split isolates true foundations again; this page now holds only token-bound boards and no shared component shells | treat this page as the true foundation source of truth, not a catch-all canonical build surface |
| `DS Primitives` | `1037:312` | 24 top-level frames spanning `Button`, `Input / Search`, `Card`, `Dropdown`, `Badge`, `Select`, `Textarea`, `Avatar`, `Switch`, `Modal`, `LoadingSkeleton`, and paired `Icon` foundations boards | 2026-04-30 page-role split moved primitive component boards off `DS Foundations`; the same day added canonical `Avatar / Foundations` light/dark boards plus nested `Avatar / Size` and `Avatar / Source` sets, the follow-up slice added paired `Switch / Foundations` boards with reusable state sets, the next heavier-primitive slice added paired `Modal / Foundations` boards plus nested `Modal / Size` sets, the next narrow follow-up added paired `LoadingSkeleton / Foundations` boards plus nested `LoadingSkeleton / Shape` sets, and the 2026-05-01 icon follow-up added paired `Icon / Foundations` usage boards that lock the shared Phosphor-light adapter contract without creating a glyph catalog | use this page only for bounded visual primitives; both `RevealImage` and `ToastProvider` are now deliberately deferred because their real contracts stay container-owned or behavior-owned rather than board-owned |
| `DS Components` | `1037:313` | paired canonical light/dark boards for `Surface`, `FormSection`, `DataPanelTable`, `EmptyState`, `SectionHeader`, `NotificationItem`, `NotificationButton`, `PickerControls`, `FileUploadWidget`, and `Delivery method toggle`, plus adjacent product-study boards | 2026-04-30 page-role split moved composed shared-component boards off `DS Foundations` without changing their live node ids; the first composed follow-up slice then added paired `EmptyState / Foundations` boards plus nested light/dark source sets, the next slice added paired `SectionHeader / Foundations` boards plus nested light/dark source sets, the 2026-05-01 admin-notification slice added paired `NotificationItem / Foundations` boards plus light/dark source sets, the next product-bound follow-up added paired `NotificationButton / Foundations` boards plus light/dark count source sets, the bounded admin-picker follow-up added paired `PickerControls / Foundations` boards, the upload-shell follow-up added paired `FileUploadWidget / Foundations` boards, and the next creator-delivery follow-up added paired `Delivery method toggle / Foundations` boards | use this page for composed shared surfaces and bounded product exports instead of mixing them with tokens or primitive boards |
| `DS Parking / Legacy` | `1037:314` | 1 top-level parking frame: `Recovered DataPanelTable root text` | recovered non-canonical root text is intentionally parked here so it stops polluting the live library pages | do not treat this page as canonical DS coverage |
| `Foundation Review` | `1041:312` | 1 top-level review frame: `Foundation Review / Light` (`1041:313`) | the 2026-04-30 page-role split restored a dedicated review-only surface after canonical pages were separated by role | keep this page as a review surface, not a reusable library source |

### Section Inventory

| Page | Section node | Section | Audit status | Notes |
| --- | --- | --- | --- | --- |
| `DS Foundations` | `47:2` | `Typography / Light` | verified | 2026-04-28 section re-audit confirms all text nodes stay bound for font family, font size, line height, and text fill, and the five section cards bind their 24px corners through per-corner `radius/lg` variables rather than local radius drift |
| `DS Foundations` | `2:1176` | `Typography / Dark` | verified | 2026-04-28 section re-audit confirms all text nodes stay bound for font family, font size, line height, and text fill, and the five section cards bind their 24px corners through per-corner `radius/lg` variables rather than local radius drift |
| `DS Foundations` | `2:1269` | `Color Primitives / Light` | verified | 2026-04-28 section re-audit confirms the board stays fully bound for text, fills, strokes, displayed hex labels, and per-corner radius. The live primitive collection now includes `29` color variables after extending the dedicated support-status family to `support/success/*`, `support/warning/*`, and `support/info/*` in `base / dust / soft` steps. |
| `DS Foundations` | `158:177` | `Color Primitives / Dark` | verified | 2026-04-28 section re-audit confirms the board stays fully bound for text, fills, strokes, displayed hex labels, and per-corner radius. The live primitive collection now includes `29` color variables after extending the dedicated support-status family to `support/success/*`, `support/warning/*`, and `support/info/*` in `base / dust / soft` steps. |
| `DS Foundations` | `147:2` | `Spacing + Radius / Primitives` | verified | 2026-05-02 primitive follow-up confirms the board stays fully bound for text, fills, strokes, spacing samples, and per-corner radius, now including both the `radius/xs` and `radius/sm+` sample rows alongside the existing `space/2` primitive binding. |
| `DS Primitives` | `191:2` | `Button / Foundations / Light` | verified | 2026-05-02 button follow-up confirms the light board stays fully bound for text family/size/line-height/fill and for shell fills, strokes, and per-corner radius across the live `Button / State`, `Button / Size`, and `Button / Icon` sets. The earlier `wrapper radius debt` note remains closed: the container frames and icon-instance shells do not carry any non-zero local radius drift. The current foundations board now carries a dedicated `RowAction / State` companion inside `Button States`, sourced from the live `Edit / Open` posture instead of reusing `quiet`, while the recipe card keeps `Row action` as an `Edit / Open` example row plus that same `Default / Hover / Focus / Pressed / Disabled` state strip, locks `Pagination item` to the same rounded-rect `radius/sm (8px)` posture, and keeps `Panel CTA` on that same rounded-rect table posture via the calmer bounded-neutral `soft` tone. Canonical Figma now treats `tertiary` as the fourth family tone: text-first neutral, no fill/rest border, no hover underline, and icon derivatives whose Phosphor glyph fills and strokes match the label token instead of carrying local accent color. |
| `DS Primitives` | `294:29` | `Button / Foundations / Dark` | verified | same token-bound posture as the light board. The 2026-05-02 re-audit confirms the old dark `light recipe` subtitle drift is still gone, the dark foundations board mirrors the same dedicated `RowAction / State` companion, and no non-zero local radius debt remains in the dark `Button / State`, `Button / Size`, or `Button / Icon` sets. The same follow-up also closes the temporary tertiary-icon drift: the dark `Button / Icon` glyph strokes now bind to the same neutral token as the tertiary labels instead of keeping a stale local orange rail. |
| `DS Primitives` | `1377:280` | `Icon / Foundations / Light` | verified | 2026-05-01 icon follow-up adds a canonical usage board for the shared runtime icon adapter rather than a glyph catalog. The light board locks `Phosphor` as the family source, `light` as the default weight, the shared `14 / 16 / 20 / 24` size ladder, semantic tones (`fg/default`, `fg/muted`, `fg/subtle`, `fg/on-fill-dark`, `fg/on-fill-light`), and bounded context examples (`dense row action`, `default UI`, `support action`, `section support`). Glyph choice stays product-owned and flows through `@/lib/icons`; this board proves usage rules only. |
| `DS Primitives` | `1377:394` | `Icon / Foundations / Dark` | verified | same contract as the light board under the dark surface shell. The paired board confirms the same Phosphor-light usage system and semantic tone ladder in dark mode without promoting a separate `Icon` primitive family or copying the external glyph library into the canonical file. |
| `DS Components` | `1403:74` | `NotificationItem / Foundations / Light` | verified | 2026-05-01 notification follow-up adds a bounded admin status-shell board to `DS Components`. The light board proves only the compact notification item shell plus light `NotificationItem / Variant / Source` set (`1409:160`) with `type=success|info|warning|error` and `action=no-action|with-action`; the current shell posture is the refined compact pass: a `325px` card width, shared `title-sm` (`16/24`) title copy, `14/20` secondary copy plus inline action text, `radius/sm (8px)`, `32px` status wrappers with `20px` Phosphor-light glyphs, and an inline `20px` dismiss affordance instead of the older absolute close button. Bell-dropdown behavior, unread counts, stack choreography, and queue timing remain intentionally out of scope. The info posture now binds dedicated `support/info/soft` wrapper fills plus `support/info/base` glyph color instead of borrowing the primary family, and the title rung should bind through dedicated `type/title-sm/size` + `type/title-sm/line` variables instead of local `16/24` overrides. |
| `DS Components` | `1403:99` | `NotificationItem / Foundations / Dark` | verified | same bounded contract as the light board under the dark shell, with the paired dark `NotificationItem / Variant / Source` set (`1435:2289`) and the same refined compact shell posture (`325px` item width, shared `title-sm` (`16/24`) title copy, `14/20` secondary copy/action text, `8px` radius, `20px` glyphs, inline dismiss affordance). The dark info posture now uses the same dedicated `support/info/soft` wrapper and `support/info/base` glyph contract instead of reusing `primary/*` variables, and the compact title rung should bind through dedicated `type/title-sm/size` + `type/title-sm/line` variables. |
| `DS Components` | `1489:194` | `NotificationButton / Foundations / Light` | verified with explicit token gaps | 2026-05-02 product-bound follow-up adds a bounded bell/count board to `DS Components`. The light board proves only the compact topbar trigger shell plus light `NotificationButton / Variant / Source` set (`1496:72`) with `count=0|3|9+`; the current shell posture is the runtime-owned compact pass: `40px` circular bell shell, `20px` Phosphor-light bell glyph, optional unread badge at `20px`/`24px` width, and a count wrapper that sits as a sibling of the button shell instead of being folded into the icon lane. Bell dropdown behavior, unread state transitions, and notification-row rendering remain intentionally out of scope. The current token gaps are explicit: runtime still uses `border-border-strong` and a hardcoded unread red, while canonical Figma currently binds the shell to `border/default` and the badge to `state/danger-fill`. |
| `DS Components` | `1489:284` | `NotificationButton / Foundations / Dark` | verified with explicit token gaps | same bounded contract as the light board under the dark shell, with the paired dark `NotificationButton / Variant / Source` set (`1496:143`) and the same compact trigger posture (`40px` bell shell, `20px` glyph, optional `20px`/`24px` unread badge, sibling badge wrapper outside the icon lane). The same two token gaps remain explicit on the dark board too: the shell still binds `border/default` because `border/strong` is not exposed in the canonical file, and the badge still uses `state/danger-fill` because no dedicated unread-badge semantic exists yet. |
| `DS Components` | `1498:194` | `PickerControls / Foundations / Light` | verified with explicit token gaps | 2026-05-02 bounded admin-picker follow-up adds a canonical picker-shell board to `DS Components`. The light board now proves the bounded admin `images branch` first, then the shared action-row affordances, compact preview/media shells, and default/active/reject dropzone posture that the runtime owner actually exports. The nested `Image links` editor is shown there as context only, not as a promoted DS primitive. Upload progress, selected-file metadata, and async workflow messaging remain intentionally route-owned. The current token gaps are explicit rather than silent: runtime still uses stronger border aliases plus local danger/red emphasis in some picker states, while canonical Figma currently stays on `border/default`, `primary/base`, and `state/danger-*` bindings. |
| `DS Components` | `1498:257` | `PickerControls / Foundations / Dark` | verified with explicit token gaps | same bounded contract as the light board under the dark shell. The dark board now locks the same composed `images branch` plus shared action-row, preview/media, and dropzone scope with explicit dark variable modes instead of inheriting the light mode accidentally. The same token gaps remain explicit here too: canonical Figma still uses `border/default`, `primary/base`, and `state/danger-*` bindings because stronger border aliases and a dedicated picker-danger semantic are not exposed in the current file. |
| `DS Primitives` | `315:56` | `Input / Search / Light` | verified with explicit wrapper/token gaps | 2026-04-28 control re-audit confirms the light board stays fully bound for text family and text fills, shell fills/strokes, and field-shell radius across the live `Input / State`, `Input / Size`, `SearchInput / State`, and `SearchInput / Size` sets. The remaining live gaps are narrow and explicit: the four component-set wrappers still keep local `cornerRadius=5`, and the `Clear` action label in the search-state explainer still uses local `14/20` typography while staying bound for font family and text fill. |
| `DS Primitives` | `360:2` | `Input / Search / Dark` | verified with explicit wrapper/token gaps | same token-bound posture as the light board and the same two remaining gaps: local `cornerRadius=5` on the four component-set wrappers plus a local `14/20` `Clear` action label in the search-state explainer. |
| `DS Primitives` | `439:110` | `Card / Foundations / Light` | verified with explicit token gaps | 2026-04-28 shell re-audit confirms the light board stays bound for font family, line height, text fill, shell fills/strokes, and per-corner radius on the section cards plus the live `Card / Size / Source` set. The remaining card gaps are explicit rather than structural: local type sizes still drive the title/body/footer copy, and the geometry keeps intentional local values such as `space/20`, `space/10`, and the preview-stack arrangement. |
| `DS Primitives` | `726:156` | `Card / Foundations / Dark` | verified with explicit token gaps | same remaining token-gap posture as the light board, now tracked under the current live dark frame id. The dark board still uses the same source-set geometry and the same local type-size gap, and the old wrapper-radius debt claim remains closed. |
| `DS Primitives` | `499:110` | `Dropdown / Foundations / Light` | verified with explicit token gaps | 2026-04-28 shell re-audit confirms the light board stays fully bound for all `23/23` text nodes across font family, font size, line height, and text fill, and all painted fills/strokes on the board remain token-bound. The remaining Figma-only gap is now narrowed to explicit local radius on the study containers `context scene` (`20`) and `row calibration scene` (`20`) plus the default/unselected `menu row` shells (`12`). |
| `DS Primitives` | `499:181` | `Dropdown / Foundations / Dark` | verified with explicit token gaps | same token-bound posture as the light board and the same narrow local-radius gap: `context scene` + `row calibration scene` stay on local `20`, while the default/unselected `menu row` shells keep local `12`. |
| `DS Components` | `573:143` | `Surface / Foundations / Light` | verified with explicit token/copy gaps | 2026-04-28 shell re-audit plus the follow-up tone/radius sync confirm the light board stays fully bound for all `26/26` text nodes across font family, font size, line height, and text fill, all painted fills/strokes remain token-bound, and the hierarchy `shell zone` now binds to `radius/lg (24px)` rather than keeping a local radius. The hierarchy demo now mirrors the source shell ladder directly too: `panel = bg/surface`, `subtle = bg/canvas`, `muted = bg/inset`, and the inner `panel / subtle / muted / popover / support` shells all sit on `16px` radius. The board copy itself remains partially stale. |
| `DS Components` | `573:182` | `Surface / Foundations / Dark` | verified with explicit token/copy gaps | same token-bound posture as the light board, tracked under the live dark frame id. The hierarchy demo now matches the source shell ladder, the `16px` inner-surface posture, and a bound `radius/lg (24px)` shell zone, while the dark source/hierarchy ids live under `627:633` and `627:646`. The explanatory board copy remains broader/staler than the live nodes. |
| `Foundation Review` | `1041:313` | `Foundation Review / Light` | review-only | keep as a comparison board; it should not be treated as a reusable library source or a proof that every review text node already mirrors the canonical font-family variable |

### Variable Collection Audit

| Collection | Modes | Variable count | Current prefix groups |
| --- | --- | --- | --- |
| `Color / Primitives` | `Light`, `Dark` | 26 | `neutral` (8), `primary` (4), `accent` (8), `support` (6) |
| `Color / Semantic` | `Light`, `Dark` | 22 | `bg` (4), `border` (1), `fg` (5), `action` (5), `focus` (1), `state` (6) |
| `Typography / Primitives` | `Base` | 11 | `font` (3), `type` (8) |
| `Typography / Semantic` | `Base` | 15 | `type` (15) |
| `Spacing / Primitives` | `Base` | 9 | `space` (9) |
| `Radius / Primitives` | `Mode 1` | 7 | `radius` (7) |

## Registry Rules

- Every shared DS component that is added to the live Figma library should get
  a row here.
- Every new row must include:
  - Figma page
  - Figma node ID
  - canonical code path
  - ownership category
  - mapping status
- Use repo-canonical paths, not compatibility wrappers.
- Prefer the implementation owner path even if the component is re-exported from
  `@/design-system`.
- For reusable Figma component-set properties, prefer code-style prop naming:
  - property names should mirror code props such as `variant`, `size`, `align`,
    `submitButton`
  - variant option values should stay lower-case when the code prop values are
    lower-case, such as `primary`, `outline`, `sm`, `md`, `flat`, `card`
- When a component is renamed in Figma or code, update this registry in the same
  change.
- When a component is removed from Figma or code, remove or mark the row in the
  same change.

## Status Legend

- `mapped-manual`: Figma component exists and its code owner is recorded here.
- `pending-figma`: code exists, but a production-quality Figma component is not
  in the live library yet.
- `pending-code`: Figma component exists, but no canonical code owner has been
  chosen yet.
- `legacy`: do not use for new work.

## Canonical File Inventory Snapshot

First-pass repo-vs-Figma diff against the canonical file
`koZEgVUfQhNEQmXISNQx56` (`Krukraft Theme Lab Source-of-Truth`) as of
`2026-04-30`.

This snapshot is the working migration ledger for the current
`Inventory diff + docs drift` phase. Use it to distinguish current canonical
coverage from older row/node claims that still need to be re-established in the
new file.

| Area | DS items | Classification | Canonical Figma evidence | Action |
| --- | --- | --- | --- | --- |
| Tokens | `colors.ts`, `spacing.ts`, `radius.ts`, `typography.ts` | `mapped and current` | `DS Foundations` contains color, spacing/radius, and typography boards plus matching variable collections | carry forward as the current foundation baseline |
| Tokens | `hero.ts` | `code exists, figma missing` | no hero-token board or hero-specific variable collection in the canonical file | leave out of the foundation pass; revisit with product exemplars |
| Foundations | `Button` | `mapped and current` | canonical file contains reusable `Button / State`, `Button / Size`, and `Button / Icon` component sets in light/dark coverage; the current Figma source uses `primary | quiet | soft | tertiary`, trials `sm=36` on the size boards, and keeps tertiary icon glyph fill/stroke on the same neutral token as the label. Runtime now mirrors that canonical posture too, with `ghost` retained only as a compatibility alias. | carry forward as the current foundation slice with runtime/Figma parity landed for the shared family; remaining follow-up is cleanup of deprecated aliases, not visual contract drift |
| Foundations | `PillLink` | `mapped and current` | canonical file now contains paired `PillLink / Foundations / Light` and `PillLink / Foundations / Dark` boards under `DS Primitives` | carry forward as the bounded section-header browse-link foundation slice; keep empty-state pill links and card-footer text CTAs route-owned until they are promoted intentionally |
| Foundations | `EmptyStatePillLink` | `code exists, figma missing` | no canonical empty-state pill-link board exists in the current file yet | keep runtime-first for now; revisit only if the bordered empty-state secondary-action shell is promoted into its own foundations board |
| Foundations | `Input`, `SearchInput` | `mapped and current` | canonical file now contains light/dark `Input / State`, `Input / Size`, `SearchInput / State`, and `SearchInput / Size` component sets under the `Input / Search` foundation boards | carry forward as the current field-shell foundation mapping; the 2026-04-27 re-audit locked both families to `radius/sm (8px)` across state + size coverage, while hero/loading/submit-button coverage remains follow-up scope |
| Primitives | `Card` | `mapped and current` | canonical file now contains `Card / Size` and `Card / Size / Dark` component sets under dedicated `Card / Foundations` light/dark boards | carry forward as the first shared-library shell proof point and reuse it when remapping generic surface posture |
| Primitives | `LoadingSkeleton` | `mapped and current` | canonical file now contains paired `LoadingSkeleton / Foundations / Light` and `LoadingSkeleton / Foundations / Dark` boards plus reusable `LoadingSkeleton / Shape` sets | carry forward as a shell-only primitive slice; keep content-block, table-row, hero, and detail loading geometry route-owned while leaving the `bg/muted` semantic token gap explicit |
| Primitives | `Dropdown` | `mapped study reference` | canonical file now contains verified light/dark dropdown-popover shell study boards, but it still does not expose a reusable component set or final mapping node | close the current shared-library pass with `Dropdown` intentionally kept as a study-board reference; reopen only when a true reusable component-set mapping is ready |
| Primitives | `RevealImage`, `ToastProvider` | `code exists, figma missing` | no canonical pattern or component for either item in the current file | both items are now intentionally deferred after inventory: `RevealImage` because its visual contract stays container-owned, `ToastProvider` because its shared contract is provider behavior/timing rather than a bounded static primitive board |
| Composed | `FormSection` | `mapped and current` | canonical file now contains dedicated `FormSection / Foundations / Light` and `FormSection / Foundations / Dark` boards plus reusable `FormSection / Variant / Source` light/dark sets for `flat` and `card` | carry forward as the first composed shared-form shell on top of `Surface` and `Card`; keep the explicit typography/spacing/border token gaps visible |
| Composed | `Pagination`, `RowActions`, `ConfirmDialog` | `doc drift` | older rows still point at pages/nodes from the previous file, while the current canonical file still does not yet verify these three components | revisit only if a separate optional composed follow-up plan is opened |
| Composed | `DataPanelTable` | `mapped and current` | canonical file now contains dedicated `DataPanelTable / Foundations / Light` and `DataPanelTable / Foundations / Dark` boards plus reusable `DataPanelTable / Variant / Source` light/dark sets | carry forward as the canonical dashboard/admin table shell; keep row schema, filters, and business actions route-owned while leaving the `border/subtle` token gap explicit |
| Composed | `Surface` | `mapped and current` | canonical file now contains verified `Surface / Foundations / Light` and `Surface / Foundations / Dark` boards with a `Surface / Variant / Source` block plus a hierarchy study card | carry forward as the shared shell bridge before remapping `FormSection` and `DataPanelTable`; keep the remaining token gaps explicit |
| Product-bound | `ResourceCard`, `PriceBadge`, `PriceLabel` | `code exists, figma missing` | no canonical marketplace pricing/card exemplar in the current file | not ready for numeric-contract seeding; defer until one bounded marketplace card/pricing source set is promoted as the canonical product exemplar |
| Product-bound | `FileUploadWidget` | `mapped and current` | canonical file now contains paired `FileUploadWidget / Foundations / Light` and `FileUploadWidget / Foundations / Dark` boards as one bounded upload-shell exemplar | carry forward as the canonical buyer-file upload shell contract; keep upload progress, validation, upload-result states, and selected-file metadata route-owned until those states are promoted as separate bounded follow-ups |
| Product-bound | `PickerControls` | `mapped and current` | canonical file now contains paired `PickerControls / Foundations / Light` and `PickerControls / Foundations / Dark` boards as one bounded admin picker exemplar | carry forward as the canonical picker-shell contract; keep upload progress, selected-file metadata, and async workflow copy route-owned while leaving stronger-border and danger-semantic token gaps explicit |
| Layout | `Container`, `PageContainer`, `PageContent`, `PageContentWide`, `PageContentNarrow`, `SidebarContainer`, `SidebarNav`, `SidebarSection`, `SidebarSectionLabel`, `IconWrapper`, `SidebarBadge`, `SidebarItem`, `SidebarFooter`, `Divider`, `NavGroup` | `code exists, figma missing` | no layout-helper page or verified layout primitive board exists in the current canonical file | decide later whether these should gain Figma coverage or remain code-owned/documented only |
| Review-only Figma surface | `Foundation Review / Light` | `figma exists, code missing` | review board exists in the canonical file but is not a code component | keep as a review surface, not a DS mapping target |

## Primitives

| Figma page | Figma node | Figma component | Code path | Owner | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `DS Primitives` | `746:510`, `553:167`, `259:92`, `1276:960`, `1276:1021`, `1276:1058` | `Button` | `src/design-system/primitives/Button.tsx` | DS primitive | `mapped-manual` | Canonical file now verifies light/dark `Button / State`, `Button / Size`, and `Button / Icon` sets with the current live ids. The 2026-05-02 control re-audit keeps the stale `wrapper radius debt` claim closed too: the component sets do not carry any non-zero local radius drift, and the dark subtitle drift is gone. The current canonical Figma base keeps `primary | quiet | soft | tertiary`, still documents the `sm=36` ladder, and now documents recipes more explicitly: `Button States` itself carries dedicated light/dark `RowAction / State` companions sourced from the live `Edit / Open` posture, `Row action` keeps an `Edit / Open` example row plus that same `Default / Hover / Focus / Pressed / Disabled` state strip in the recipe card, `Pagination item` shares the same rounded-rect `radius/sm (8px)` posture, and `Panel CTA` follows that same rounded-rect posture on the calmer bounded-neutral `soft` tone instead of the older primary-pill candidate. `quiet` still carries its own `border/quiet` stroke token derived from `primary/lift`, while `tertiary` intentionally drops fill and rest border entirely: it is a text-first neutral action that stays on `fg/muted` at rest, shifts to `fg/default` on hover/pressed, keeps `fg/subtle` on disabled, reserves a `focus/ring` stroke only for focus, and keeps its icon derivatives on the same neutral label token for both glyph fill and stroke. Runtime now mirrors that canonical family directly: `Button.tsx` exposes `tertiary`, keeps `sm=36`, and retains `ghost` only as a thin compatibility alias while `RowActionButton` continues to follow the dedicated row-action state ladder directly at both `sm` and `md` sizes (`border/default` rest shell, `bg/inset` hover/pressed, `border primary` focus, `fg/subtle` disabled) and `PaginationButton size=\"md\"` continues to carry the matching rounded-rect page-item posture. |
| `DS Primitives` | `736:178`, `1494:1803` | `Badge` | `src/design-system/primitives/Badge.tsx` | DS primitive | `mapped-manual` | Canonical file now provides verified `Badge / Variant / Source` and `Badge / Variant / Source / Dark` sets inside dedicated `Badge / Foundations` light/dark boards. The canonical set keeps `Badge` non-interactive and covers `neutral`, `info`, `success`, `warning`, `featured`, `destructive`, and `outline`. The latest badge re-audit confirms the boards stay fully bound for font family/text fill and for all painted fills/strokes, and the warm split still holds: `warning` stays on `bg/inset` with `support/warning/base` stroke and `support/warning/dust` text, while `featured` now uses `accent/sand/wash` fill with `accent/sand/dust` stroke and `accent/sand/base` text in both light and dark. The 2026-04-29 residual cleanup closed the remaining Figma-side badge gaps too: the light/dark source-set wrappers now sit at `cornerRadius=0`, and the seven badge labels now bind to dedicated `type/badge/size` + `type/badge/line` variables instead of local `12/16` overrides. The boards now also carry a usage-only `resource-card badge proofs` block that proves the approved product mapping without expanding the primitive family: top-left status uses `Owned`, `New`, and `Featured`, while top-right highlight uses `Trending` plus one generic highlight posture (`Recommended`). Runtime `Badge.tsx` now mirrors the same canonical set directly, with the earlier runtime-only aliases removed from the shared primitive contract, the primitive locking the same `12/16` badge recipe at the root via shared badge font-size/line-height vars so color variants cannot strip it during class merge, enforcing the canonical `24px` shell height directly, `featured` consuming the canonical sand family directly, and the status variants using dedicated theme-aware runtime vars for `support/info/*`, `support/success/*`, `support/warning/*`, and `state/danger/*`. |
| `DS Primitives` | `1757:368`, `1757:411` | `Chip` | `src/design-system/primitives/Chip.tsx` | DS primitive | `mapped-manual` | Canonical file now provides paired `Chip / Foundations / Light` and `Chip / Foundations / Dark` boards, and runtime now mirrors that bounded family through the shared `Chip.tsx` primitive. The slice stays intentionally narrow and fully token-bound: `navigation`, `filter`, and `removable` rows each prove one bounded state strip only, selected/active shells keep one shared selected token family across rows, and `pending` now stays subtly distinct from `active` instead of duplicating it. Runtime now aliases the selected family through `--state-selected-fill` and `--state-selected-stroke`, while active chip labels stay on `fg/default`, instead of rebinding chip-active UI directly to raw `primary/*` utilities, and adoption now spans marketplace category/search chips, dashboard library filters, admin removable filters and analytics presets, plus resource-detail token links through that same `40px` shell. |
| `DS Primitives` | `1842:392`, `1842:450` | `PillLink` | `src/design-system/primitives/PillLink.tsx` | DS primitive | `mapped-manual` | Canonical file now provides paired `PillLink / Foundations / Light` and `PillLink / Foundations / Dark` boards under the current live ids. The slice stays intentionally narrow and fully token-bound: it proves `section-header pill-link` only, with `default`, `hover`, `focus-visible`, and `disabled` states on the compact `32px` shell. The current source sets now live at light `1842:408` and dark `1842:466`, follow the standard foundations page pattern (`usage card 24/8`, `variants card 32/24`, `source-set shell 12/12`), keep a `View all` label plus shared `ArrowRight` instance, and lock shell geometry to `padding 4/12/4/16`, `gap 8`, `icon 14`. State bindings are `default = action/primary`, `hover = bg/inset + fg/default`, `focus-visible = focus/ring 2px + action/primary`, and `disabled = fg/subtle`. Runtime mirrors that bounded family through `PillLink.tsx`, and current live adoption stays intentionally scoped to resources/discover section-header browse actions such as `View all`, `Browse everything`, and `Open marketplace`. Empty-state pill links and card-footer text CTAs remain route-owned until they are promoted into a separate family or variant. |
| `not-built` | `not-built` | `EmptyStatePillLink` | `src/design-system/primitives/EmptyStatePillLink.tsx` | DS primitive | `pending-figma` | Runtime now splits the bordered empty-state browse/recovery action out of `PillLink` instead of stretching the section-header family. Current live adoption is intentionally narrow: `ResourceGrid` and `categories/[slug]` reuse the same bordered `40px` shell for `Clear filters` and `Explore all resources`, while section-header browse links stay on the compact `32px` `PillLink` family and retry/error action pairs stay on `Button`. Promote to Figma only if the empty-state secondary-action shell is intentionally canonized as its own foundations slice. |
| `DS Primitives` | `432:218`, `548:134`, `627:780`, `627:804` | `Input` | `src/design-system/primitives/Input.tsx` | DS primitive | `mapped-manual` | Canonical file now provides normalized light/dark `Input / State` and `Input / Size` sets with the current live ids. The 2026-04-28 control re-audit confirms that every field shell in those sets still uses `radius/sm (8px)` with the shared `field` height ladder, `space/16`, and caption-scale text. The remaining Figma-only gaps are explicit: the light/dark component-set wrappers keep local `cornerRadius=5`, and the search-explainer `Clear` text keeps a local `14/20` typography override. The 2026-04-29 residual follow-up now closes the shared runtime field-shell drift too: `Input.tsx` enforces the same `8px` radius at runtime, with `/admin/users` as the first proof route. |
| `DS Primitives` | `994:342`, `994:366`, `994:519`, `994:543` | `Select` | `src/design-system/primitives/Select.tsx` | DS primitive | `mapped-manual` | Canonical file now provides `Select / Foundations / Light` and `Select / Foundations / Dark` boards plus dedicated `Select / State` and `Select / Size` sets under the current live ids. The mapping intentionally mirrors the shared field-shell grammar proven in `Input / Search`: `radius/sm (8px)`, shared field heights, helper/error copy below the shell, and an explicit caret affordance on every state/size variant while option lists remain route-owned. The first runtime parity slice is now live too: `Select.tsx` explicitly keeps the same `8px` radius target at runtime, `/admin/settings` is the first proved route family because it mounts `Input`, `Select`, and `Textarea` together without product-owned search overrides, `admin/resources` is the first widened follow-up family for shared select shells, and the low-risk admin filter bucket (`/admin/activity`, `/admin/audit`, `/admin/analytics/ranking`) now proves the same shared select geometry on select-only routes. |
| `DS Primitives` | `1019:386`, `1019:507` | `Textarea` | `src/design-system/primitives/Textarea.tsx` | DS primitive | `mapped-manual` | Canonical file now provides `Textarea / Foundations / Light` and `Textarea / Foundations / Dark` boards plus dedicated `Textarea / State` sets under the current live ids. The multiline contract intentionally reuses the same quiet field-shell target as `Input / Search` and `Select` (`radius/sm (8px)`, helper/error copy below the shell) while keeping exact rows, max length, counters, and AI-specific affordances route-owned. Unlike `Select`, `Textarea` does not invent a fake size ladder in Figma; the canonical slice proves state choreography only and documents min-height / resize behavior as route-owned shell guidance. The first runtime parity slice is now live too: `Textarea.tsx` keeps the same `8px` radius target at runtime, `/admin/settings` proves the roomy multiline shell without widening product-owned long-form behaviors, and `admin/resources` is now the first widened follow-up family for shared multiline shells. |
| `DS Primitives` | `439:303`, `726:171` | `Card` | `src/design-system/primitives/Card.tsx` | DS primitive | `mapped-manual` | Canonical file now provides first-pass light/dark `Card / Size / Source` component sets with `size=default` and `size=sm`, plus dedicated `Card / Foundations` study boards for shell posture reference under the current live ids. Repo runtime adoption started on 2026-04-28 too: the shared `Card` primitive now renders its root shell through `surface` and its footer through `inset`, matching the current Figma base more directly. The remaining Figma-side debt is explicit instead of structural: local type sizes on the title/body/footer copy plus the intentional local geometry values (`space/20`, `space/10`, preview-stack arrangement). |
| `DS Primitives` | `1087:246`, `1087:292`, `1087:350`, `1087:363` | `Switch` | `src/design-system/primitives/Switch.tsx` | DS primitive | `mapped-manual` | Canonical file now provides paired `Switch / Foundations / Light` and `Switch / Foundations / Dark` boards plus reusable `Switch / State` sets under the current live ids. The slice intentionally stays narrow: it locks the current runtime `46×24` track, `20×20` thumb, and the four visual states (`unchecked`, `checked`, `disabled unchecked`, `disabled checked`) while leaving labels, helper copy, row shells, and form composition route-owned. Focus remains documented as a runtime accessibility affordance rather than a Figma source variant, and no broader control-size ladder is invented around the switch yet. |
| `DS Primitives` | `499:110`, `499:181` | `Dropdown` | `src/design-system/primitives/Dropdown.tsx` | DS primitive | `mapped-study` | Canonical file now contains verified `Dropdown / Foundations / Light` and `Dropdown / Foundations / Dark` study boards covering trigger context, open shell, selected row, submenu edge, and destructive row. The 2026-04-28 shell re-audit confirms those boards are fully bound for text family/size/line-height/fill and for all painted fills/strokes; the only remaining Figma-only debt is the intentional local radius on `context scene` + `row calibration scene` (`20`) and the default/unselected `menu row` shells (`12`). `Dropdown` should stay out of numeric-contract enforcement for now: the runtime owner also bundles checkbox/radio/submenu behavior and there is still no reusable source-set/component-set mapping that locks one canonical implementation truth. |
| `DS Primitives` | `1063:246`, `1063:367`, `1063:317`, `1063:366`, `1063:438`, `1063:487` | `Avatar` | `src/design-system/primitives/Avatar.tsx` | DS primitive | `mapped-manual` | Canonical file now provides `Avatar / Foundations / Light` and `Avatar / Foundations / Dark` boards plus nested `Avatar / Size` and `Avatar / Source` component sets under the current live ids. The source slice locks the shared 24/32/40/56 ladder and the current runtime fallback order (image, name-derived initials, email-derived initials, explicit initials, anonymous default). Image and fallback states now share the same stroked circular shell posture in both Figma and code. The remaining gap stays narrow and explicit: runtime-only 28/36/72/104 mounts still sit outside the canonical ladder, and the fallback shell keeps a local brand gradient plus proportional initials sizing until avatar-specific gradient/type tokens exist. |
| `DS Primitives` | `1146:246`, `1154:246`, `1146:352`, `1276:1255` | `Modal` | `src/design-system/primitives/Modal.tsx` | DS primitive | `mapped-manual` | Canonical file now provides `Modal / Foundations / Light` and `Modal / Foundations / Dark` boards plus nested `Modal / Size` sets under the current live ids. The 2026-05-01 re-audit confirms the refreshed light/dark boards still lock the same bounded primitive contract rather than route-specific form content: `sm`, `md`, `lg`, and `xl` prove the `384 / 448 / 512 / 576` width ladder, the centered surface frame, header/body/footer rail structure, and the default close affordance posture that backs the real `/admin/categories` consumer. The footer CTA pair shown in the boards remains illustrative shell proof only, not primitive-owned button semantics. The current Figma-only gap is explicit rather than silent: divider rails still bind to `neutral/line` because the canonical file does not yet expose a separate semantic `border/subtle` variable, while overlay tint and portal motion stay runtime-owned concerns. |
| `DS Primitives` | `1170:246`, `1170:272`, `1170:302`, `1170:307` | `LoadingSkeleton` | `src/design-system/primitives/LoadingSkeleton.tsx` | DS primitive | `mapped-manual` | Canonical file now provides `LoadingSkeleton / Foundations / Light` and `LoadingSkeleton / Foundations / Dark` boards plus nested `LoadingSkeleton / Shape` sets under the current live ids. The slice intentionally stays shell-only: it proves the neutral pulse primitive and a bounded `line | bar | circle | pill` posture set without promoting content-block, table-row, hero, or detail loading layouts into the primitive contract. The current Figma-only gap is explicit: there is still no dedicated semantic `bg/muted` variable in the canonical file, so the light board binds shell fills to `bg/inset` and the dark board binds them to `border/default` to mirror the current runtime aliasing. |
| `DS Primitives` | `1377:280`, `1377:394` | `Icon` | `src/lib/icons.tsx` | DS adapter | `mapped-manual` | Canonical Figma coverage now exists as paired `Icon / Foundations / Light` and `Icon / Foundations / Dark` usage boards rather than a standalone `Icon` primitive component. The slice intentionally documents the adapter contract only: `Phosphor` family source, default `light` weight, a bounded `14 / 16 / 20 / 24` size ladder, semantic tone ladder (`fg/default`, `fg/muted`, `fg/subtle`, `fg/on-fill-dark`, `fg/on-fill-light`), and bounded context examples (`dense row action`, `default UI`, `support action`, `section support`). Runtime ownership stays in `src/lib/icons.tsx`; glyph choice remains product-owned and no full glyph catalog should be promoted into the canonical file. |
| `DS Primitives` | `432:276`, `548:166`, `627:821`, `627:842` | `SearchInput` | `src/design-system/primitives/SearchInput.tsx` | DS primitive | `mapped-manual` | Canonical file now provides normalized light/dark `SearchInput / State` and `SearchInput / Size` sets with the current live ids. The 2026-04-28 control re-audit confirms the same `radius/sm (8px)` field shell across all four sets, plus the same narrow wrapper/token gaps tracked on the parent `Input / Search` boards: local `cornerRadius=5` on the component-set wrappers and a local `14/20` `Clear` action label in the search-state explainer. The 2026-04-29 runtime parity slice now adopts that shell on `SearchInput variant=\"default\"` itself: `/dashboard/library` proves the shared `40px / 8px` toolbar search, the dashboard topbar search on that same `40px / 8px` ladder, the hydrated topbar clear action, and the admin global/users/resources/activity/ranking mounts now route through the same primitive instead of local `Input + icon` wrappers. Public `/resources` remains a route-owned product override with `40px` height and pill geometry. `hero`, `loading`, and route-owned search-shell overrides remain outside this foundation-first mapping. |
| `Primitives` | `not-built` | `RevealImage` | `src/design-system/primitives/RevealImage.tsx` | DS primitive | `deferred-after-inventory` | The runtime helper only wraps `next/image` for already-sized containers and lets the caller own shell geometry, crop mode, placeholder tone, overlay, and decoration. Real mounts in `ResourceGallery`, `ResourceCard`, and `HeroSearch` prove product-/container-owned image behavior rather than a bounded shared Figma primitive contract, so canonical board coverage should stay deferred for now. |
| `Primitives` | `not-built` | `ToastProvider` | `src/design-system/primitives/ToastProvider.tsx` | DS primitive | `deferred-after-inventory` | The runtime primitive is a provider + queue manager whose important contract is behavioral: global mount ownership, viewport anchoring, success-dedupe, auto-dismiss timing, persistent warning/error defaults, and exit choreography. With no canonical `Toast` or `Notification` nodes in the Figma file, a static primitive board would under-describe the real shared contract, so canonical Figma coverage should stay deferred for now. |

## Composed

| Figma page | Figma node | Figma component | Code path | Owner | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `DS Components` | `759:156`, `759:184`, `759:251`, `746:275` | `FormSection` | `src/design-system/components/FormSection.tsx` | DS composed | `mapped-manual` | Canonical file now provides `FormSection / Foundations / Light` and `FormSection / Foundations / Dark` boards plus `FormSection / Variant / Source` light/dark sets under the current live ids for `variant=flat|card`. The 2026-05-02 follow-up keeps that variant contract narrow, closes the earlier optional-structure proof gap with usage-only `flat / no-description` and `card / no-footer` examples, and replaces the old manual footer action frames with true DS `Button` instances. The boards are now fully bound for live paints, text properties, radius, and spacing. The remaining explicit gap is no longer unbound-token debt but parity choice: live Figma normalizes several former local values to the closest shared tokens (`type/label 14/20`, `space/8`, `space/24`), while runtime still holds narrower literal geometry in places, and divider/footer separators still rely on the broader `border/default` token until a dedicated `border/subtle` semantic exists. |
| `DS Components` | `1214:68`, `1214:93`, `1214:141`, `1276:1353` | `SectionHeader` | `src/design-system/components/SectionHeader.tsx` | DS composed | `mapped-manual` | Canonical file now provides `SectionHeader / Foundations / Light` and `SectionHeader / Foundations / Dark` boards plus light/dark `SectionHeader / Variant / Source` sets under the current live ids. The 2026-05-01 composed-slice pass locks the bounded shared shell only: eyebrow, title, description, alignment, and the optional trailing actions slot through a `default | centered | with-actions | minimal` source set that mirrors runtime plus Storybook posture. The eyebrow examples now bind the shared `badge` size contract (`12/16`) instead of the larger label-sized posture, while the `with-actions` lane reuses canonical `Button size=md` instances from the shared button family with generic `Quiet action` / `Primary action` labels so the board proves a DS-backed action cluster without inventing route-specific CTA semantics. |
| `DS Components` | `813:174`, `813:227`, `813:545`, `1276:78` | `DataPanelTable` | `src/design-system/components/DataPanelTable.tsx` | DS composed | `mapped-manual` | Canonical file now provides `DataPanelTable / Foundations / Light` and `DataPanelTable / Foundations / Dark` boards plus `DataPanelTable / Variant / Source` light/dark sets under the current live ids. The 2026-04-28 shared-component re-audit confirms the source stays shell-scoped and progressive: it proves `actions=false, toolbar=false, footer=false`, `actions=true, toolbar=false, footer=false`, `actions=true, toolbar=true, footer=false`, and `actions=true, toolbar=false, footer=true` while keeping column schema, row rendering, empty-state content, and business actions route-owned. The related table-action guidance now splits more clearly: `RowAction / State` has its own bounded light/dark family plus the restored `example row + state strip` guidance seen in the canonical `Button recipes` card, and the live light/dark `DataPanelTable` source sets now reuse those real `RowAction / State` default instances directly in every row-action lane instead of `Button / Size` quiet placeholders. `Pagination item` keeps the same rounded-rect `radius/sm (8px)` posture as a recipe instead of a new family, and the table-shell panel CTA examples now follow that same `8px` posture on the quieter `soft` tone instead of a primary-pill outlier. The first runtime rollout slice now uses that recipe in the dashboard creator-resources table by switching the `Edit` action to `RowActionButton size=\"md\"` and the page controls to `PaginationButton size=\"md\"`. The remaining live Figma-only gaps are explicit instead of broad: the two `Showing latest 2 entries` footer-note copies still sit fully local, the light/dark source-set wrapper frames still keep local `cornerRadius=5`, and the table copy itself still relies on local type recipes (`16/20` titles, `14/20` descriptions/columns/row copy, inherited `12/16` badge sizing from the shared badge recipe) while runtime still asks for `border-subtle` and route-owned table-head fills. |
| `not-built` | `not-built` | `Pagination` | `src/design-system/components/Pagination.tsx` | DS composed | `pending-figma` | Previous mapping came from the older DS file; rebuild and remap after the foundation pass. |
| `DS Components` | `1192:68`, `1192:121`, `1192:196`, `1276:302` | `EmptyState` | `src/design-system/components/EmptyState.tsx` | DS composed | `mapped-manual` | Canonical file now provides `EmptyState / Foundations / Light` and `EmptyState / Foundations / Dark` boards plus light/dark `EmptyState / Variant / Source` sets under the current live ids. The 2026-04-30 composed-slice pass locked the bounded shared shell only: dashed rounded container, centered stack rhythm, and `icon | title | description | action` slot order. The latest creator-upload follow-up extends that source set to `default | minimal | compact-admin`: `compact-admin` promotes the tighter inline empty posture from the creator-side `Image links` branch without dragging the full editor workflow into `EmptyState`. The action lane still reuses a canonical `Button size=sm` soft instance with a generic `Soft action` label, and the icon example still reuses a bounded `Phosphor light` instance, so the board proves a DS-backed CTA + icon posture without inventing route-specific product chrome. The remaining explicit Figma-only gap is still narrow: runtime asks for `border-border-subtle`, but canonical Figma still lacks a semantic `border/subtle` variable, so the dashed rail currently binds to `border/default`. |
| `DS Components` | `1403:74`, `1403:99`, `1409:160`, `1435:2289` | `NotificationItem` | `src/components/admin/NotificationItem.tsx` | Admin bounded surface | `mapped-manual` | Canonical file now provides `NotificationItem / Foundations / Light` and `NotificationItem / Foundations / Dark` boards plus light/dark `NotificationItem / Variant / Source` sets under the current live ids. The 2026-05-01 notification slice keeps scope intentionally narrow: `type=success|info|warning|error` proves wrapper posture, `action=no-action|with-action` proves the same compact shell with and without an inline follow-up label, and the board locks the bounded shell only: `325px` compact width, shared `title-sm` (`16/24`) title copy, `14/20` secondary copy plus action text, `8px` shell radius, `32px` status wrappers with `20px` glyphs, and the compact inline `20px` dismiss affordance. The info status posture now uses dedicated `support/info/soft` and `support/info/base` variables in the canonical file instead of borrowing the primary family, and the compact title rung should bind through dedicated `type/title-sm/size` + `type/title-sm/line` variables instead of local `16/24` overrides. Bell dropdown behavior, unread counts, stack order, and queue timing remain runtime-owned and intentionally stay out of this board. |
| `not-built` | `1329:310`, `1276:1005` | `RowActions` | `src/design-system/components/RowActions.tsx` | DS composed | `mapped-manual` | There is still no dedicated `DS Components` board for `RowActions`, but canonical Figma coverage now exists through the shared light/dark `RowAction / State` sets plus the `Button recipes / Row action` example cards. The live `DataPanelTable` source sets now reuse those same default row-action instances directly, so the family is already exercised on a canonical composed surface even before a standalone `RowActions` board exists. |
| `not-built` | `not-built` | `ConfirmDialog` | `src/design-system/components/ConfirmDialog.tsx` | DS composed | `pending-figma` | Previous mapping came from the older DS file; rebuild and remap after the foundation pass. |
| `DS Components` | `575:144`, `627:633`, `575:157`, `627:646` | `Surface` | `src/design-system/components/Surface.tsx` | DS composed | `mapped-manual` | Canonical file now contains verified light/dark `Surface / Variant / Source` blocks plus paired hierarchy study cards under the current live ids. The current live shell ladder is explicit and aligned across both surfaces: source variants bind `panel = bg/surface`, `subtle = bg/canvas`, `muted = bg/inset`, and the hierarchy demo mirrors that same tone split while the inner `panel / subtle / muted / popover / support` shells all sit on `16px` radius and the broader `shell zone` binds to `radius/lg (24px)`. The board copy still mentions a broader token-gap story (`border/subtle`, `bg-muted`, `radius/12`, `space/20`) than the live nodes now show, so treat that text as stale guidance rather than as the authoritative node-level truth. |

## Product-Bound DS Exports

| Figma page | Figma node | Figma component | Code path | Owner | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `Product / Marketplace` | `not-built` | `ResourceCard` | `src/components/resources/ResourceCard.tsx` | Product DS export | `pending-figma` | Not ready for numeric-contract seeding. Marketplace card mappings still need one refreshed canonical Figma surface that locks shell, media, pricing, and metadata together; the current hero banner is only the stable marketplace exemplar. |
| `Product / Marketplace` | `not-built` | `PriceBadge` | `src/design-system/components/PriceBadge.tsx` | Product DS export | `pending-figma` | Not ready for numeric-contract seeding. It still depends on a canonical marketplace pricing surface rather than a standalone bounded board. |
| `Product / Marketplace` | `not-built` | `PriceLabel` | `src/design-system/components/PriceLabel.tsx` | Product DS export | `pending-figma` | Not ready for numeric-contract seeding. It still depends on a canonical marketplace pricing surface rather than a standalone bounded board. |
| `not-built` | `not-built` | `HeroBanner` | `src/components/marketplace/HeroBanner.tsx`, `src/components/marketplace/HeroSurface.tsx` | Product DS export | `pending-figma` | Current canonical file does not yet carry the previous marketplace hero exemplar; reintroduce it intentionally in the product-exemplar pass. |
| `DS Components` | `1558:270`, `1558:295` | `FileUploadWidget` | `src/design-system/components/FileUploadWidget.tsx` | Product DS export | `mapped-manual` | Canonical file now provides `FileUploadWidget / Foundations / Light` and `FileUploadWidget / Foundations / Dark` boards under the current live ids. The current bounded upload-shell pass keeps scope intentionally narrow: one buyer-file upload branch proves the shared dropzone shell, helper-copy rhythm, and disabled CTA posture only. Upload progress, validation, upload-result states, selected-file metadata, and broader admin workflow hooks remain runtime-owned and intentionally stay out of this board for now. |
| `DS Components` | `1562:2`, `1562:25` | `Delivery method toggle` | `src/components/creator/CreatorResourceForm.tsx` | Product control contract | `mapped-manual` | Canonical file now provides `Delivery method toggle / Foundations / Light` and `Delivery method toggle / Foundations / Dark` boards under the current live ids. The current bounded pass keeps scope intentionally narrow: one buyer-file source toggle proves the segmented shell, compact helper-copy rhythm, and active/inactive posture only. The upload branch, external-link branch, validation copy, and downstream file workflow remain route-owned and intentionally stay out of this board. |
| `DS Components` | `1489:194`, `1489:284`, `1496:72`, `1496:143` | `NotificationButton` | `src/design-system/components/NotificationButton.tsx` | Product DS export | `mapped-manual` | Canonical file now provides `NotificationButton / Foundations / Light` and `NotificationButton / Foundations / Dark` boards plus light/dark `NotificationButton / Variant / Source` sets under the current live ids. The 2026-05-02 product-bound follow-up keeps scope intentionally narrow: `count=0|3|9+` proves the compact topbar bell shell, the `20px` Phosphor-light bell glyph, and the sibling unread-badge posture only. Bell dropdown behavior, unread transitions, and notification-row rendering remain runtime-owned and intentionally stay out of this board. The current token gaps are explicit rather than silent: runtime still uses `border-border-strong` and a hardcoded unread red, while canonical Figma currently binds the shell to `border/default` and the badge to `state/danger-fill`. |
| `DS Components` | `1498:194`, `1498:257` | `PickerControls` | `src/design-system/components/PickerControls.tsx` | Product DS export | `mapped-manual` | Canonical file now provides `PickerControls / Foundations / Light` and `PickerControls / Foundations / Dark` boards under the current live ids. The 2026-05-02 bounded admin-picker follow-up keeps scope intentionally narrow but now proves the bounded admin `images branch` first, then the shared action-row posture, compact preview/media shells, and `default|active|reject` dropzone states that the runtime owner actually exports. The nested `Image links` editor remains context-only inside the board; upload progress, selected-file metadata, file-preview business copy, and async workflow messaging remain route-owned and intentionally stay out of this board. The current token gaps are explicit rather than silent: runtime still uses stronger border aliases plus local danger/red emphasis in some picker states, while canonical Figma currently stays on `border/default`, `primary/base`, and `state/danger-*` bindings. |

## Product Flow Exemplars

These are owner-page flow exemplars, not one-to-one Code Connect candidates.

| Figma page | Figma node | Pattern | Backing source | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| `not-built` | `not-built` | `Flow / Admin Settings` | `src/components/settings/*`, `src/app/admin/settings/AdminSettingsClient.tsx` | `pending-figma` | Previous flow exemplar came from the older file; re-establish only if this pattern becomes part of the new canonical file. |
| `not-built` | `not-built` | `Flow / Hero Banner` | `src/components/marketplace/HeroSurface.tsx`, `src/components/marketplace/HeroBanner.tsx` | `pending-figma` | Previous flow exemplar came from the older file; re-establish during the product-exemplar pass if marketplace hero remains in scope. |
| `not-built` | `not-built` | `Flow / Creator Dashboard` | `src/app/(dashboard-v2)/dashboard-v2/creator/*` | `legacy` | Historical row from the retired dashboard-v2 route family; do not treat it as a current canonical Figma mapping target. |

## First Manual Mapping Set

If the team stays on Figma Professional and keeps using manual mapping instead
of Code Connect, the minimum set to keep accurate is:

- `Button`
- `Badge`
- `Input`
- `Select`
- `Textarea`
- `Card`
- `FormSection`
- `SectionHeader`
- `Pagination`
- `EmptyState`
- `Surface`

## Change Checklist

When adding a new shared component in code:

1. Add or update the canonical implementation under `src/design-system/*` if it
   is a generic DS surface.
2. Add or update the matching component in the live Figma library.
3. Add or update the row in this file.
4. Update `/design-system.md`, `src/design-system/README.md`, and
   `krukraft-ai-contexts/06-design-system.md` if the change affects shared
   ownership or handoff understanding.
5. Run `npm run figma-map:check` before closing the task.

When adding a new component in Figma first:

1. Decide whether it is a generic DS surface or a product-bound export.
2. Pick the canonical code owner path.
3. Add the row here immediately, even if the code component is still pending.
4. Do not leave anonymous or temporary Figma components without a registry row
   once they are intended to be reused.
5. Run `npm run figma-map:check` after the corresponding code surface is added
   or renamed.
